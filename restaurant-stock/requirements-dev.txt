-r requirements.txt
# NR-12 (débordements mobiles) et les parcours hors-ligne de F3 tournent
# dans un vrai Chromium via Playwright. Ces tests sont ignorés proprement
# s'il est absent : la suite reste exécutable partout.
# Après installation : `playwright install --with-deps chromium`.
playwright>=1.45
