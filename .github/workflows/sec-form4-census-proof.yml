name: SEC Form-4 census proof staging guard

on:
  workflow_dispatch:

jobs:
  staging-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'SEC census proof intentionally disabled only during exact Git payload staging.'
