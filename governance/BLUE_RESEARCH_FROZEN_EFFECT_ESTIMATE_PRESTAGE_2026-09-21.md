# BLUE — RESEARCH -> FROZEN EFFECT ESTIMATE SCIENTIFIC PRESTAGE — 2026-09-21

## 0. Authority

Mission type:

`READ_ONLY_ANALYSIS + SCIENTIFIC_CONTRACT_PREP`

Purpose:

close the final non-code prerequisite before the first large Product/Economic vertical build:

`REAL_RESEARCH_TO_FROZEN_EFFECT_ESTIMATE = MISSING_SCIENTIFIC_OUTPUT_CONTRACT`

This mission does not authorize Product implementation.

Current consolidated pre-big-build correction authority:

`governance/BLUE_POST_P0_VERTICAL_PREBIGBUILD_CORRECTION_SPEC_2026-09-21.md`

## 1. Exact question

Determine whether existing Quant science/Research code already computes, or can
truthfully expose without changing its scientific claim, the exact frozen Economic
effect coordinate consumed by:

`src/quant/economics/decision.py :: EffectEstimate`

The mission must identify the real producer, not design a convenient heuristic.

## 2. Required frozen output contract

A valid Research output must establish:

- `delta_hat`;
- `lower`;
- `upper`;
- `confidence_level`;
- `evidence_label`;
- `sample_provenance`;
- `event_count` where available;
- `p_value` where available;
- `clustering_unit_provenance`;
- exact `DeltaCoordinateBinding`;
- `evaluate_delta_coordinate(binding).compatible == True`;
- exact allocation-constructor identity;
- proof that allocation weights precede outcomes;
- point estimate and interval on the same coordinate;
- security and SPY benchmark return conventions on the same interval/treatment.

A literal matching `estimator_form` string is insufficient.

## 3. Required source archaeology

Inspect exact current/pinned code, at minimum:

Canonical Economic:
`parallel/claude-economic-v2-2026-09-20@35dff27b8fac53618da434ee6d31febbddcc0e69`

including:
- `src/quant/economics/decision.py`
- `src/quant/economics/coordinate.py`
- `src/quant/economics/recipe.py`
- `src/quant/economics/states.py`

Canonical Forward:
`parallel/claude-forward-data-2026-09-20@83521dbfdd90027c90d04adfb7d814593c2355c5`

including relevant:
- `src/quant/science/`
- `src/quant/dataplane/`
- forward admissibility/provenance surfaces.

Blue Product/Research spine:
`cbf30c1bb38dd69c11fb64c523c3aec694116b9c`

including:
- `src/autonomous_research/ticket.py`
- `src/quant/factory/evaluate.py`
- `src/quant/factory/workers.py`
- strategy/dataset provenance surfaces.

Search for actual implementations/signatures containing concepts such as:
- formation;
- inference;
- bootstrap;
- confidence interval;
- clustering;
- allocation weights;
- benchmark/SPY;
- event return;
- excess return;
- point estimate;
- estimator;
- delta coordinate.

Do targeted reads. Do not burn tokens rereading unrelated repository material.

## 4. Falsification questions

For every candidate producer determine:

1. Does it estimate exactly EC1's frozen `delta`, or merely something correlated?
2. Is the aggregation exactly `ALLOCATION_WEIGHTED_RATIO`?
3. Are allocation weights fixed before outcomes?
4. Are point and interval generated by the same estimand?
5. Are security and benchmark return conventions identical and declared?
6. Is the benchmark SPY?
7. Is the holding interval identical on both sides?
8. Are corporate actions and terminal/delisting treatment declared?
9. Is clustering scientifically justified, not merely named?
10. Is sample provenance immutable and PIT-safe?
11. Was the same evidence already consumed for discovery/validation?
12. Can the output legally claim FORWARD_CONFIRMATION, or only DEVELOPMENT?
13. Does multiple-testing/selection alter interval interpretation?
14. Does any transform silently change gross vs net-of-friction semantics?

Do not infer equivalence from similar variable names or units.

## 5. Allowed outcomes

### Outcome A — existing lawful producer

If exact code already produces the required coordinate:

document:
- exact producer file/function;
- exact input contract;
- exact formulas/semantics;
- exact output fields;
- exact DeltaCoordinateBinding;
- exact provenance;
- minimum adapter into ResearchTicket.

Then:

`SCIENCE_EFFECT_ESTIMATE_CONTRACT = READY_FOR_BUILD_SPEC`

### Outcome B — nearly complete producer

If existing code computes the correct scientific object but does not expose all
required metadata:

identify only the smallest missing output/interface extension.

Do not redesign the estimator.

Then:

`SCIENCE_EFFECT_ESTIMATE_CONTRACT = READY_WITH_BOUNDED_OUTPUT_EXTENSION`

### Outcome C — no lawful producer

If current code does not compute the frozen coordinate:

state this plainly.

Specify exactly what is missing scientifically, without inventing the computation.

Then:

`SCIENCE_EFFECT_ESTIMATE_CONTRACT = BLOCKED_MISSING_SCIENTIFIC_ESTIMATOR`

The future vertical build must retain real Research -> Economic as fail-closed
NO_TRADE until separately resolved.

## 6. No implementation

Do NOT modify:

- `src/`;
- `tests/`;
- `scripts/`;
- schemas;
- workflows;
- P0 runtime;
- target host.

Do not merge/cherry-pick branches.

Only one final handoff may be written.

## 7. Required output

Write:

`handoff/CLAUDE_RESEARCH_FROZEN_EFFECT_ESTIMATE_PRESTAGE_2026-09-21.md`

Include:

- exact refs inspected;
- candidate producer inventory;
- rejected candidate producers + reason;
- exact winning producer, if any;
- formal field-by-field mapping to `EffectEstimate`;
- exact `DeltaCoordinateBinding`;
- interval/clustering provenance;
- PIT/evidence-label constraints;
- smallest required future changed paths;
- precise impact on the one-big-build mission;
- remaining unresolved science questions.

Final status must be one of the three Outcome statuses above.

## 8. Safety

```text
PRODUCT_CODE_MODIFIED = FALSE
PRODUCT_INTEGRATION = PAUSED
P0_RUNTIME_MUTATION = FALSE
TARGET_HOST_TOUCHED = FALSE
GATE_B_MUTATION_AUTHORIZED = FALSE
t0 = NOT_DECLARED
REAL_CAPITAL_AUTHORIZED = FALSE
```
