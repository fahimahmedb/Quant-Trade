# BLUE ROUTE-B CHECKPOINT ADDENDUM — DIRECT FULL-SUPPORT / SINGLE ROBUST UCB — 2026-09-18

**Status:** CURRENT CHECKPOINT ADDENDUM  
**Authority:** Blue Team / Mission Control  
**Parents:** `BLUE_ROUTE_B_PHASE_COMPLETE_PERSISTENCE_CHECKPOINT_ADDENDUM_2026-09-18.md`, `D05A_D09_DIRECT_FULL_SUPPORT_SINGLE_ROBUST_UCB_SIMPLIFICATION_AMENDMENT_2026-09-18.md`

This addendum controls where more recent/specific than its parent checkpoint.

## 1. Active estimator

`ACTIVE_BREACH_ESTIMATOR = DIRECT_FULL_SUPPORT_WEIGHTED_FREQUENCY`.

For every W and x:

`B_hat_W(x) = sum_{i,t} a_{i,t} I_i,t(W,x)`

on the one frozen common calibration support / target measure.

## 2. Phase/thinning status

The phase-complete construction is retained as historical governance evidence only.

`PHASE_THINNING_AUTHORITY = RETIRED_CURRENT_LINEAGE`.

Reason: the phase-complete estimator is algebraically identical to the direct estimator while cross-phase rolling-overlap covariance remains in the authority-bearing uncertainty problem.

No phase object is required on the active calibration path.

## 3. Persistence diagnostic status

`D_PERSIST_METHOD_BRANCHING = RETIRED_CURRENT_LINEAGE`.

A persistence metric may be reported descriptively, but it cannot select the authority method.

No diagnostic branch threshold or intermediate-case rule is required.

## 4. Single UCB architecture

`ACTIVE_UCB_ARCHITECTURE = SINGLE_SYNCHRONIZED_CALENDAR_X_CORPORATE_METHOD_CLASS`.

The exact final method must:
- preserve rolling-window / serial dependence through a frozen block/bandwidth rule;
- use common calendar draws across the full 2K family;
- preserve corporate-dependence grouping;
- produce W-specific simultaneous one-sided UCBs;
- control familywise error across all frozen windows and x in {0,1}.

A synchronized max-statistic construction is preferred to Bonferroni if the final theorem supports it.

## 5. Dependence-length rule

Block/bandwidth selection remains open but is one nuisance-calibration rule within the single UCB method, not a method selector.

The rule must be frozen before its empirical value and cannot be tuned by W pass/fail results.

## 6. Capacity / failure state

Uniform selector authority still requires all frozen windows to meet the predeclared precision design.

If any W is underpowered:

`LIQUIDITY_BREACH_CALIBRATION_UNDERPOWERED`.

No post-hoc window deletion, diagnostic branching or alternative UCB method may rescue the same lineage.

## 7. Revised immediate path

1. freeze W_SET / W_MAX / common calibration support and weights;
2. freeze numerical P_MAX_REF / P_TARGET_REF / BETA_LIQ_MAX / TAU_BETA / ALPHA_BETA;
3. derive EPSILON_ADV_BAR;
4. freeze exact synchronized calendar x corporate UCB theorem;
5. freeze its dependence-length / block-bandwidth rule;
6. derive pre-result precision/capacity requirement;
7. only then inspect external available capacity;
8. if sufficient, compute simultaneous one-sided UCB_W(x);
9. apply shortest-passing endpoint rule and lambda materiality discharge.

Route-B firewall remains unsatisfied.
D05 ceiling visibility remains unauthorized.
Form-4 outcome access remains unauthorized.
