# BLUE D05/D07 Governance Checkpoint — 2026-09-15

**Project:** Quant / Agent Trader  
**Authority:** Blue Team / Mission Control  
**Status:** CHECKPOINT / INDEX

This file is a compact index of the canonical governance artifacts listed below. It is **not** a substitute for their full text.

The Git commit containing this checkpoint freezes the **governance rules as repository content**. It does **not** by itself freeze any empirical population, dataset or expected-accession manifest.

For the denominator contract specifically:

- the canonical contract file freezes the **projection and reconciliation rule**;
- a future independently built and hashed SEC-index manifest will freeze the **realized expected population**;
- the manifest must be hashed before any payload join result or mismatch is observed.

## 1. Canonical artifacts

The following files are canonical for the current Blue D05/D07 state:

1. `governance/D07_OPEN_SPACE_BOUNDARY.md`
2. `governance/D05_OBSERVATION_UNIT_TAXONOMY.md`
3. `governance/D05_REPRESENTATIVENESS_FAILURE_MODE_REGISTER.md`
4. `governance/D05_DENOMINATOR_RECONCILIATION_CONTRACT.md`

If this checkpoint summary conflicts with one of those full artifacts, the full artifact controls.

All remain subordinate to `QUANT_NORTH_STAR.md`, closed Blue decisions, frozen scientific contracts and certified artifacts.

## 2. Qualification notation — explicit disambiguation

The frozen Form 4 transaction qualification is:

- non-derivative;
- **transaction code = `P`**;
- **acquired/disposed indicator = `A` (acquired)**.

This does **not** admit transaction code `A` (award/grant).

Every canonical artifact uses this distinction explicitly.

## 3. Current closed/frozen state

- `D07_OPEN_SPACE_BOUNDARY`: **FROZEN**
- D05 Observation Unit Taxonomy: **CLOSED**
- Representativeness Failure-Mode Register: **CLOSED**
- Denominator Reconciliation **rule**: **FROZEN**
- Expected SEC-index manifest/population: **NOT YET CREATED / NOT YET HASHED**
- `ALLOWABLE_UNBOUNDED_DENOMINATOR_UNKNOWN = 0`: **FROZEN**
- D05-A empirical execution: **NOT AUTHORIZED**
- D07 final geometry: **NOT FROZEN**
- outcome access: **NOT AUTHORIZED**

## 4. Core D07 state

Closed claim elements include:

- original Form 4;
- non-derivative;
- transaction code `P`;
- acquired/disposed indicator `A`;
- officer/director qualification;
- reporting-owner CIK identity;
- issuer CIK identity;
- two distinct qualifying insider CIKs;
- 10 regular sessions;
- `<2 -> >=2`;
- re-arm below two;
- EDGAR date as public-observability fact;
- first regular-session open after EDGAR date as economic entry;
- 20-session hold;
- SPY primary scientific benchmark.

Open D07 dimensions remain:

- O1 formation-session projection for no-session EDGAR dates;
- O2 deterministic same-session ordering;
- O3 20-session interval indexing;
- O4 overlap/statistical observation geometry.

## 5. D05 authority model

Unit classes:

- U0 `SOURCE_PRIMITIVE`
- U1 `FROZEN_RULE_DERIVED`
- U2 `RESOLUTION_DERIVED`
- U3 `D07_DESIGN_DERIVED`

Destinations:

- `D05-A`
- `D05-B`
- `OUT_OF_PASS`

Two independent gates:

1. design-invariance;
2. frozen inspection surface.

Key rule:

`CLASSIFY_BY_PRODUCER`.

## 6. Failure-mode-derived surface

The D05-A inspection surface must be derived from named representativeness failure modes, not from available variables.

Registered families:

- FM-01 technical source availability;
- FM-02 identity/security resolution;
- FM-03 market-observation coverage;
- FM-04 denominator incompleteness;
- FM-05 parsing/ingestion integrity;
- FM-06 documentary/regulatory regime shift;
- FM-07 reference-regime drift.

Hidden concentrated missingness is a cross-cutting reading rule, not a separate failure mode.

## 7. Coverage requirement principles

Separate:

`Coverage Requirement Generator`

from:

`Observed Coverage State`.

Requirement levels must be anchored outside observed D05-A coverage, including:

- MEUE/power requirements;
- estimand bias sensitivity;
- concentrated-missingness bounds;
- D19 rules;
- conservatism margin.

Core invariants:

- `COVERAGE_REQUIREMENT_INDEPENDENCE`
- `D05A_INFORMS_APPLICATION_NOT_LENIENCY`
- `INSUFFICIENT_MUST_BE_REACHABLE`
- `ALLOWABLE_UNBOUNDED_DENOMINATOR_UNKNOWN = 0`

## 8. Denominator architecture

Canonical architecture:

`OFFICIAL INDEX`
→ `HASHED EXPECTED MANIFEST`
→ `EXPECTED`
→ `PAYLOAD JOIN`
→ `INGESTED`
→ `PARSED`
→ `{QUALIFYING | NON_QUALIFYING | QUALIFICATION_INDECIDABLE}`
→ `RESOLVED`
→ `COVERED`.

Authority and transport are separate:

`TRANSPORT_DOES_NOT_DEFINE_SCOPE`.

The expected population is filing-date bounded by:

`2020-01-01 <= OFFICIAL_INDEX.DATE_FILED <= 2026-06-30`

inclusive.

Transaction date is an observed property, not a scope criterion.

The join has explicit J0–J5 states.

Denominator certainty and ingestion completeness are orthogonal:

`DENOMINATOR_CERTAINTY_ORTHOGONAL_TO_INGESTION_COMPLETENESS`.

## 9. Current implementation consequence

Current A1 historical acquisition primarily uses quarterly SEC Form 3/4/5 bulk archives, while the Q3-2026 tail already uses `master.idx`.

Before D05-A:

1. independently build the official SEC index manifest under the canonical contract;
2. hash/freeze that manifest **before any join**;
3. join existing bytes by accession;
4. assign `source_index_date_filed` from the manifest;
5. classify J0–J5;
6. keep post-2026-06-30 filings outside the authority-bearing population even if their transaction dates fall in H1.

Existing bytes are transport assets, not scope authority.

## 10. What remains before D05-A

Three Blue objects remain:

1. **Coverage Requirement Generator**
2. **Minimal D05-A metric and stratum surface**
3. **Stopping rule**

Then:

`D05-A PROTOCOL FREEZE`
→ `BUILD + HASH EXPECTED MANIFEST`
→ `EXECUTE D05-A`
→ instantiate/freeze `Theta_coverage`
→ `D07 FREEZE`
→ `D05-B`
→ feasibility verdict.

No profitability or capital-authority conclusion is asserted by this checkpoint.

The North Star remains the terminal authority:

> Quant exists to increase real capital through repeatable discovery, selection, sizing, execution and replacement of genuine market edge, with net economic gain after real frictions as the terminal objective.
