# Quant System State

This file is the compact current state of the whole Quant project. It is updated at meaningful
milestones from actual evidence, not from intentions.

Read `QUANT_NORTH_STAR.md` before interpreting this file. `CHIEF_BRIEF.md` is generated
automatically from persistent state and carries the live numbers.

<!-- BEGIN GENERATED CANONICAL V1 STATUS -->
## Canonical V1 status checkpoint

This block is generated by `python3 scripts/status_artifacts.py --write` from a clean
persistent replay of the committed V1 inputs. CI reconstructs the same state and fails if
either this checkpoint or `CHIEF_BRIEF.md` drifts from it.

- Proof inventory: **478 unit tests discovered**; **35 end-to-end demo assertions**.
- Control/Desk: status **IDLE**, desk cursor `2026-09-10`, **377 executable desk sessions**.
- Data Plane: `{'AVAILABLE': 2, 'STALE': 0, 'INVALID': 0, 'MISSING': 0}`.
- Research queue: `{'PENDING': 0, 'RUNNING': 0, 'BLOCKED': 3, 'COMPLETED': 2, 'FAILED': 0}`.
- Capital Book: NAV **1,000,000.00 USD**, **377** marked sessions, **0** fills, **0** open positions; last mark `2026-09-11`.
- Evaluation ledger (zero authority): NAV **993,757.74** (-0.62%) over **377** sessions; counterfactual P&L **-6,242.26**.
- Desk tickets: `{'BOOKED': 76, 'NO_TRADE': 301}`.
- Rejection quality: **1** evaluated, **0** true rejects, **0** false rejects, **1** undetermined.
<!-- END GENERATED CANONICAL V1 STATUS -->

## Architectural state

Quant System V1 is implemented end to end in persistent paper/shadow mode. The planes named in
`SYSTEM_ARCHITECTURE.md` all exist in code, hold persistent state and are connected by the
Control Plane clock rather than by a script that runs them in order.

Package layout mirrors the planes:

| Module | Plane |
| --- | --- |
| `src/quant/clock.py`, `state.py`, `events.py`, `paths.py` | Control Plane |
| `src/quant/dataplane/` | Data Plane |
| `src/quant/factory/` | Research Factory |
| `src/quant/desk/` | Capital Desk |
| `src/quant/book/` | Persistent Book |
| `src/quant/learning/` | Learning / Memory |
| `src/quant/status/` | Status surface and Chief Brief |

The PR #7 and PR #9 primitives are reused rather than replaced: `ResearchTicket` carries research
lifecycle, and `PersistentQueue` / `ResearchTask` are now the Control Plane's durable work queue.

## What exists today

- a whole-system clock with boot/resume, durable run history, heartbeat, lease-based watchdog,
  pause/resume, fault isolation, bounded retry, dead-work protection, and a long-running `serve`
  mode that waits while IDLE and wakes on due work or newly arrived data;
- a Data Plane with adapters, validation, fingerprints, committed provenance sidecars,
  availability tracking, registry reload and dependency unblocking;
- a Research Factory with declared lanes, a discovery/validation/shadow point-in-time partition,
  a versioned strategy lifecycle that preserves superseded documents, a multiple-testing budget
  and dead-work protection;
- the `SCAN -> VET -> SIZE -> RISK -> FILLS -> BOOK` chain as six distinct, traced, stateful
  functions over an `OpportunityTicket`, on one causal timeline shared with research;
- a persistent Book with per-strategy sleeves, aggregate portfolio views, deterministic
  operation identities and monotonic marking;
- a write-ahead desk journal so an interrupted session resumes its recorded decision instead of
  re-deciding against a Book it has already mutated;
- two ledgers: the authoritative capital Book and a zero-authority evaluation ledger;
- a learning loop that scores the system's own rejections and raises `BuildTask` capability gaps;
- a status surface and `CHIEF_BRIEF.md` rendered only from persistent state, plus a CI freshness
  gate that reconstructs canonical state and rejects stale committed status artifacts;
- 104 tests, an end-to-end restart and crash demonstration with 35 assertions, and JSON schemas
  for the persistent state objects generated from the dataclasses with a drift test.

## Current data frontier

The repository is no longer data-starved for cross-sectional work. A real, credential-free
multi-asset panel is ingested, validated and committed:

- `us_sector_etf_daily`: 30,168 bars, 12 symbols, 2016-09-12 to 2026-09-11, 2,514 aligned
  sessions, from the public Yahoo Finance chart endpoint;
- `nasdaq_composite_daily`: the historical index export, normalized into the same schema.

Recorded caveats include that adjusted closes are restated retroactively for corporate actions,
that the endpoint is undocumented with no availability guarantee, and that consolidated daily
bars are not the venue-level quotes a real execution model would eventually need.

This acquisition unblocked the `statistical_arbitrage` lane, which the previous opportunity map
had ranked second and blocked for want of "a synchronized multi-asset point-in-time panel".

Three lanes remain genuinely blocked and are represented as blocked work with named dependencies
and open `BuildTask` records: `factor_residual`, `insider_filings`, `volatility_surface`.

`insider_filings` is blocked on *science*, not on acquisition. The P0 SEC/Form-4 raw-capture lane
below is running and accumulating irreversible evidence; none of it is visible to research.

## SEC Form-4 P0 raw capture

`P0_RAW_CAPTURE_OPERATIONAL = TRUE` as of 2026-09-18. The lane has made real SEC requests and the
mission's acceptance criteria are met by recorded evidence, not by assertion:

| Criterion | Live result |
| --- | --- |
| `VALID_DISCOVERY` | TRUE |
| `COVERAGE_STATE` | COMPLETE |
| `HEARTBEAT_DURABLE` | TRUE |
| `RAW_CAPTURE_DURABLE` | TRUE |

The opaque evidence artifact is `handoff/SEC_FORM4_P0_FIRST_LIVE_PROBE_2026-09-18.json`. It carries
attempt/receipt timestamps, content addresses, byte totals, the request controls in force and the
SEC policy sources consulted. It deliberately carries no filing identity, locator, body or count.

What the first live run established: the collector operated below its frozen 2 req/s cap and
SEC's documented 10 req/s ceiling; discovery validated successfully; immutable content-addressed
Form-4 captures are present and passed full address/hash verification; a subsequent poll *earned*
`NO_NEW_DATA` by re-establishing continuity from the cursor without refetching acknowledged work;
and a restart preserved the attempt journal, envelopes, raw bytes, cursor and coverage state.

Design, in one line each:

- **Discovery** is the Latest Filings Ownership/Form-4 Atom feed, one compact request per poll. The
  raw document locator is derived deterministically from the accession, so a filing costs exactly
  one further request and nothing is crawled around it.
- **`NO_NEW_DATA` is earned.** `assert_no_new_data_earned` requires a validated discovery response
  *and* proven continuity. An HTML error page under a nominal 200, an unparseable payload, a form
  filter that cannot be shown to work, or an unaccountable entry are all `DISCOVERY_INVALID`.
- **Coverage is a claim with a proof.** Continuity means the poll walked back to the exact identity
  the last validated poll reached. Everything else is `COVERAGE_UNKNOWN` with the affected
  cursor/page range or day recorded durably. A healthy heartbeat never upgrades coverage.
- **The daily master index is the coverage authority.** Expected Form-4 accessions for a closed day
  are compared against what was captured. Day-1 is honest detection; the backfill engine is later
  work, and a detected gap stays open rather than being explained away.
- **Immutability.** Content-addressed objects linked into place and stored `0o444`; an identical
  re-capture deduplicates; differing bytes under one source identity append an explicit conflict
  version; a truncated transfer is preserved in a separate incomplete namespace and can never be
  promoted to a capture. The hash always describes the bytes that arrived, so the gzip bodies SEC
  actually serves are stored compressed and decoded only on a throwaway copy for parsing.
- **Commit order** is raw bytes, then envelope, then task acknowledgement, then cursor advance. The
  envelope is the acknowledgement, which is what makes each crash boundary replayable.
- **Request policy** is deliberately below the SEC ceiling: 60s poll, concurrency 1, 2 req/s, no
  burst, bounded `5s -> 15s -> 60s -> 5m -> 15m` backoff with jitter, `Retry-After` honoured as a
  floor, extended cooldown on 403, and cooldowns that survive restart. The budget is a file lock, so
  it is global across consumers *on one machine*.
- **Fails closed.** Without `QUANT_SEC_USER_AGENT` carrying a contact address the lane is `BLOCKED`
  and makes no request. The declared identity is supplied at runtime and is not committed.

### Pre-t0 continuity instrumentation

`P0_CONTINUOUS_SERVICE_STATE = OPEN / NOT_YET_PROVEN_CONTINUOUS`. Both frozen
`BLOCKS_CAPTURE_INTEGRITY` blockers are closed, and `t0` is Blue's to declare.

- **`HIDDEN_TRANSPORT_RETRY` — CLOSED.** The nested HTTP retry is removed, and
  `ONE_BUDGET_RESERVATION == ONE_NETWORK_REQUEST_ATTEMPT == ONE_AUDITABLE_ATTEMPT_ID`
  is now structural: `transport.fetch` requires a single-use permit minted from
  one budget reservation and carrying the attempt id, consumed at send, and a
  second send raises. The ordinary stale-keep-alive case is handled by
  reconnecting *before* sending, which emits no extra request.
- **`INCOMPLETE_CRITICAL_POLICY_SERIALIZATION` — CLOSED.** A dedicated canonical
  serialization replaces `to_dict()`. Every `SecAccessPolicy` field carries
  exactly one of three disjoint classifications, checked against
  `dataclasses.fields` at generation time, so an unclassified field blocks
  fingerprint generation rather than escaping it.
- **`ACQUISITION_CRITICAL_FINGERPRINT` materialized.** `sha256(canonical_json(manifest))`
  over module digests, canonical policy, request shape, the classification itself
  and the supervisor manifest. `clock.py` is included in full per the
  conservative V1 rule.
- **Provenance.** Scheduler transitions are written prospectively with
  `next_due_at`, cause, active fingerprint and cooldown state, so the audit never
  infers that nothing was due from the absence of an attempt. Lifecycle cause
  comes from `deploy/quant_sec_supervisor.py`; an absent cause is
  `LIFECYCLE_CAUSE_UNATTESTED`, an explicit gap, never a benign default.

### Blue review of PR #17 — three further defects, closed

Blue reviewed head `d1b164fe` and did not authorize merge or `t0`. Three defects,
all accepted and closed; the audit one was the most serious.

- **`RETROSPECTIVE_AUDIT_CAN_FALSE_PASS` — CLOSED.** The audit meant to prove the
  absence of holes could whitewash a missed deadline, three ways: the attempt
  window was `abs(attempt - due) <= tolerance`, so a request made *before* a
  deadline could prove it was later met; attempts were never consumed, so one
  request could answer several obligations; and *any* later transition counted as
  supersession, including one recorded after the deadline was already missed. All
  three had one root cause — obligations had no identity. They now do:
  one obligation resolves to exactly one resolution, attempt matching is
  forward-only and consumed, and supersession requires a transition that names the
  obligation, was recorded before its due time, and carries an authorized cause.
  A journal whose commitments lack identity fails the window rather than falling
  back to matching by time.
- **`LIFECYCLE_PROVENANCE_NOT_AUTHORITATIVE` — CLOSED.** Absence of
  service-manager provenance now yields `MANUAL_START`, never `SCHEDULED_START`,
  so omitting a flag cannot buy a clean record. A supervisor that dies is detected
  by its own running marker and invocation identity rather than inheriting a clean
  start. Acquisition-critical runtime overrides are refused outright in qualifying
  mode and bound into the fingerprint manifest in any mode, because two services
  from identical code with different cadence are not the same service.
- **`VISIBILITY_FIREWALL_REGRESSION` — CLOSED, exposure irreversible.** The first
  rodage artifact published a FILING request count while asserting it carried no
  filing count; one filing costs one FILING request, so it was a filing count by
  proxy. Blue saw it. It is removed, the invariant is now stated as booleans, and
  the exposure is recorded rather than papered over. Blindness was **not**
  restored and this has no authority over any later scientific decision.

### Blue follow-up on PR #17 — two points, closed

Blue confirmed the three defects fixed and CI green on `1450af4a`, and left two
concrete points. Both are closed, and closing the second exposed a third gap.

- **`SYSTEMD_RESTART_CAUSE_AMBIGUOUS` — CLOSED.** `INVOCATION_ID` proves systemd
  launched the service but not *why*, so `systemctl restart quant-sec-capture`
  typed by a human classified as `SCHEDULED_START` and hid an operator
  intervention behind a non-invalidating cause. An automatic action must now be
  **positively demonstrated**: the only positive reasons a cleanly stopped service
  can start are a host boot — visible because the kernel's boot id changed — and
  the first start of a deployment. A restart after a failure counts as automatic
  only while it is inside the window the unit's `RestartSec` implies; past that
  systemd has given up, so a start is an operator acting on a stopped service.
  Everything else is `MANUAL_START`. Verified live: a clean stop, same host boot,
  new invocation identity was classified `MANUAL_START` and recorded invalidating.
- **`MATERIALIZED_FINGERPRINT_NOT_VERIFIED_AGAINST_ACTIVE` — CLOSED.**
  `t0_readiness()` checked only that the fingerprint file existed, never that it
  described the running build. That is how a stale fingerprint reached a published
  artifact while `visibility.py` — explicitly fingerprint-critical — had changed
  after it was computed. Readiness and telemetry now compare the two and block on
  `FINGERPRINT_MATERIALIZED_MISMATCH`.

  The check immediately earned its keep: a manifest materialized *outside* the
  service environment can never match, because effective service configuration is
  part of the manifest. The supervisor therefore materializes it itself, in the
  child environment, and only when none exists — so an existing manifest is never
  silently re-frozen and a mid-window change still surfaces as a mismatch.

A further gap found while closing those: **`SERVICE_ENTRY_POINT_OUTSIDE_FINGERPRINT`**
(`BLOCKS_CAPTURE_INTEGRITY`). `scripts/quant.py` was not in the frozen membership,
yet its `sec-serve` binds the clock's wake cadence into `system.serve()` and so
decides how often a due poll is noticed — acquisition timing in a file no digest
covered. Added to the membership, and the verification tree digest now covers
`deploy/` as well. Membership may only be narrowed before the first qualifying
`t0`, so it is widened while that is still permitted.

Final rodage: `handoff/SEC_FORM4_P0_PRE_T0_RODAGE_FINAL_2026-09-19.json`, run on the
exact frozen state. Its generator **aborts if the active and materialized
fingerprints differ**, so a stale value cannot be published again. Readiness reports
ready with no blockers; the audit reports `INVALIDATING_INTERVENTION`, which is the
correct result because the rodage deliberately performed the operator-restart
falsification.

Remediating that third defect turned up two more of the same class, both closed.

- **`DRAIN_TRANSITION_COUNT_PROXY`.** The same artifact published
  `transition_causes.DRAIN_COMPLETED` and `transition_states.DRAINING` as
  integers, and the drain path completes once per acquired filing, so both were
  the filing count under another name. The first count-proxy detector missed them
  because their key names mention no filings. Per-cause tallies are now replaced
  by the set of causes observed, and the detector matches drain and capture keys
  on the full path. Blue saw these too, so the exposure is equally irreversible.
- **`FIREWALL_TIER_CONTRACT_OVERSTATED`.** `paths.sec_firewall_safe_journals()`
  was documented as "capture state a protocol-mutating surface is allowed to
  read", and the scheduler and attempt journals were in it. Those journals carry
  no filing-identifying string — but they are per-event records and some events
  are one per filing, so counting them recovers the count. The name asserted a
  guarantee the contents did not provide, which is *why* a per-cause tally reached
  a published artifact without anyone noticing it was a filing count. Renamed to
  `sec_operator_internal_journals()` with the guarantee stated exactly: no
  identifying content, volume inferable, not publishable as they stand. The
  no-count rule binds the published projection — status surface, Chief Brief,
  `telemetry()`, the observation audit and the committed artifacts — and a test
  now holds that set to it. No new disclosure: this is the contract-clarity defect
  behind the other two.

Verification is no longer a PR claim: `.github/workflows/sec-p0-pre-t0-gate.yml`
runs the suites on the commit, and `scripts/verify_p0.py` records what ran against
a named SHA in `handoff/SEC_FORM4_P0_VERIFICATION.json`.

Second rodage evidence: `handoff/SEC_FORM4_P0_PRE_T0_RODAGE_2_2026-09-18.json`.
Qualifying mode ran live under service-manager provenance; killing the supervisor
and relaunching produced `AUTOMATIC_RESTART_AFTER_SUPERVISOR_FAILURE` where the
old code would have said `SCHEDULED_START`; a `SIGTERM` mid-poll left
`COVERAGE_UNKNOWN` with the affected page range rather than a claim of
completeness; and the obligation ledger settled every obligation with none
unexplained.

A third capture-integrity defect was found **by the rodage, not by review**:
EDGAR's Latest Filings `type` parameter is a prefix match, so `type=4` also
returns `497J`/`497K` fund filings. The validator refused the poll as
`DISCOVERY_INVALID` rather than emitting `NO_NEW_DATA`, which is how it surfaced;
the first live probe had simply landed on a window of only Form-4 entries. The
server filter is now a narrowing hint with local classification authoritative,
and the query-construction version moved the fingerprint accordingly.

Rodage evidence: `handoff/SEC_FORM4_P0_PRE_T0_RODAGE_2026-09-18.json`. Two
supervised service starts including one `AUTOMATIC_RESTART_AFTER_FAILURE`, a
stable fingerprint across the restart, `COMPLETE` coverage with no open gap, an
accountable retrospective audit, and the one-to-one request invariant verified on
live traffic. What it does **not** establish: multi-day unattended operation, the
required weekend and predeclared silence interval, live 429/403 behaviour
(deliberately not provoked against SEC and proven offline only), and multi-host
budget coordination.

`CAPTURED != VISIBLE_FOR_PROTOCOL != ADMISSIBLE_FOR_CONFIRMATION` is enforced, not just asserted:
`visibility.py` scans every surface a protocol-mutating actor can see - status surface, Chief Brief,
event log, exception text, snapshot JSON, and the firewall-safe journals - for accession numbers,
archive paths, ownership XML, transaction fields, filer identities and raw markup. Filing counts are
withheld too, so work in flight is reported as a boolean. Identifying locators, accessions and
source publication times live only in `var/sec/restricted/`, which no status surface reads, so
point-in-time reconstructability is preserved without exposing anything interpretable.

## Current research evidence

**These numbers replace the ones published in the first version of PR #12.** The earlier
metrics were computed on a close(t) -> close(t+1) interval, which credited the overnight gap
between the decision and the earliest possible fill. The corrected timeline is

`information through close(t) -> decision after close(t) -> entry at open(t+1) -> exit at open(t+2)`

and is now shared by the research evaluator, the desk and the Book. The old figures are not
comparable and should not be cited.

Two lanes ran on the new panel. The second was promoted automatically by the first lane's
diagnosis, not by a human supplying the next idea.

Windows: discovery 2016-09-12 to 2022-03-08, validation 2022-03-09 to 2025-03-11, and a shadow
window from 2025-03-12 reserved for the desk and never visible to research.

### Lane 1 - `xs_daily_relative_value`: filtered before validation

Under the corrected timeline the best of its 24 declared expressions reached a discovery Sharpe
of **-0.015**. The lane's own rule is that the validation window is only spent on a candidate
that survived discovery, so the window was preserved rather than consumed. Under the old,
gap-inflated timeline this same family reached +0.20 and was carried all the way to validation.

Its scan still showed the cost mechanism clearly: 10 of 24 expressions were gross-positive and
net-negative, at a median 157x annual turnover. That diagnosis is what promoted lane 2.

### Lane 2 - `xs_execution_aware_relative_value`: rejected out of sample

| Metric | Value |
| --- | --- |
| Expression | `xs_momentum_l21_z0.5_h5_b0.05` |
| Discovery Sharpe | +0.363 |
| OOS net return | **-6.08%** |
| OOS gross return | -1.37% |
| Modelled costs | 4.90% |
| Annual turnover | 32.9x |
| Market beta | -0.062 (benchmark +38.34% over the same interval) |
| t-statistic | -0.39 against a required **3.20** after 36 declared expressions |

**No strategy is tradable.** The capital Book has never taken a position.

What the evidence actually says:

1. The daily family does not survive its own discovery window once the return interval starts
   where an order could actually execute. Most of its apparent edge was in the gap.
2. The structural response to cost worked mechanically: turnover 157x -> 33x, modelled costs
   4.9% of the window. It did not rescue the strategy, because out of sample the residual was
   gross-negative as well.
3. Nothing is close to significant. After 36 declared expressions the Bonferroni-adjusted bar is
   t >= 3.20; the best reached -0.39.
4. Beta attribution is reported on every result. |beta| < 0.07 against a benchmark that returned
   +38.3%, so essentially none of the outcome is disguised market exposure.

The honest conclusion is unchanged in direction and stronger in degree: cross-sectional sector
relative value, as expressed here, is not a source of edge on this dataset.

## Current economic state

- Capital Book: NAV 1,000,000 USD, unchanged since inception, 0 fills, 0 positions, 377 marked
  sessions. `NO_TRADE` held for the entire shadow window because nothing was validated.
- Evaluation ledger (zero authority, counterfactual only): NAV **993,757.74**, **-0.62%** over
  377 sessions, with counterfactual P&L **-6,242.26**. Execution-time open valuation is
  authoritative; older desk figures are not comparable.
- Desk tickets: **76 booked, 301 no-trade, 0 terminal blocked**. A close is not consumed until a
  later aligned execution session exists; the current executable frontier ends at close 2026-09-10
  and the Book is marked through 2026-09-11.

Decision quality: the single evaluated rejection scored **UNDETERMINED** - the counterfactual
loss is real but below the materiality band and statistically indistinguishable from zero, so
the system does not claim its rejection was vindicated. There were no false rejects.

## Whole-system maturity

| Plane | State |
| --- | --- |
| Control Plane / Clock | **Implemented.** Boot/resume, due-work routing across planes, durable run history, heartbeat, lease-based watchdog, fault isolation, bounded retry, pause/resume, dead-work protection, and a long-running mode that waits while IDLE. |
| Data Plane | **Implemented for daily bars.** Registry, adapters, validation, fingerprints, committed provenance, availability-driven unblocking. No intraday, no point-in-time fundamentals, no corporate-action history. |
| Research Factory | **Implemented for one family.** Declared lanes, discovery/validation separation, adversarial falsification, multiple-testing budget, evidence-driven follow-up promotion. Breadth is still narrow: one signal family on one asset class. |
| Capital Desk | **Implemented.** Six distinct traced stages over an `OpportunityTicket`, execution modelled at the next session's open with spread, commission, square-root impact and ADV capacity truncation. RISK validates the scaled portfolio and re-validates the executed one. |
| Persistent Book | **Implemented.** Per-strategy sleeves with separate aggregate exposure, durable and idempotent fills, monotonic marking, realized/unrealized P&L, NAV history, attribution. Crash-plus-replay is proven equal to the uninterrupted run. |
| Learning | **Implemented for research and rejections.** Lessons, lane priorities, counterfactual scoring, `BuildTask` capability gaps. Decay and retirement transitions exist but have never fired, because no strategy has been tradable. |
| Build Plane | **Represented.** Capability gaps are first-class records; execution is still a human-triggered agent task. |
| Status UI | **Implemented as a text surface.** Every field is read from persistent state; committed status is checked against a canonical replay in CI. |

## Known limitations

- Research inference still uses independent-observation t-statistics; HAC/Newey-West or a
  block bootstrap is required before future positive evidence can authorize capital.
- Native ingestion scheduling/event subscriptions remain absent; `serve` discovers refreshes
  by polling. Historical changes inside a frozen research cohort block the lineage for review.
- The SEC traffic budget is a file lock, so it bounds every consumer on one machine but cannot
  coordinate two machines. SEC counts a requester's traffic across all of them, so two collectors
  must not run concurrently until the budget moves behind a shared service.
- SEC capture history begins at its bootstrap point. The discovery feed is a rolling window with no
  beginning, so the first poll defines the scope rather than crawling backwards; filings older than
  that point were never in scope and are the reconciliation/backfill engine's work. Detection of
  such gaps exists; repair does not.
- Captured Form-4 bytes are raw evidence only. There is no parser, no qualification logic and no
  admissibility label, and `SCAN-INSIDER-FILINGS-001` stays scientifically blocked.

- Cash earns no financing return and shorts pay no borrow. Both are modelled as zero and are
  material to a real bankroll.
- The `DECAYING` and `RETIRED` lifecycle transitions are implemented and unit-tested but have
  never fired on real evidence, because no strategy has ever been tradable. The system does not
  yet demonstrate autonomous alpha-decay management; it demonstrates the state machine that
  would carry it.
- Strategy versioning preserves superseded documents in `previous_versions`, so old evidence
  stays inspectable. It is not a full audit log: intermediate edits within one version are not
  retained.
- The evaluation ledger's materiality band is a fixed fraction of initial capital rather than of
  capital actually at risk, so a small counterfactual is reported as UNDETERMINED rather than
  scaled to the exposure that produced it.
- The execution model's spread and impact coefficients are assumed, not calibrated against
  quotes. Costs decided both research results, so this assumption carries real weight.
- Research breadth is one signal family on one asset class. Nothing here supports a conclusion
  about the Research Factory's general ability to find edge.
- The status surface is a text rendering, not an interactive control UI.
- `serve` wakes on a fixed poll interval and re-reads the dataset registry; it does not yet
  subscribe to external events.

## Highest-value next actions

1. **Breadth, not depth.** One signal family on nine ETFs is too narrow a search to conclude
   anything about the Research Factory. The next lane should be structurally independent, not
   another parameterisation of the residual.
2. **Cost realism as a first-class model.** Costs decided both results. The execution model
   should be calibrated against something better than an assumed spread.
3. **A dataset that unblocks a ranked lane.** `factor_residual` is ranked above anything currently
   executable and is blocked purely on data. `insider_filings` is now accumulating raw evidence;
   what it still lacks is the scientific protocol, not the bytes.
4. **Exercise the decay path.** Until a strategy is tradable, `SHADOW -> ACTIVE_SHADOW ->
   DECAYING -> RETIRED` is untested against real evidence.

## Builder checkpoint — first vertical shadow loop (in progress, 2026-09-21)

Mission: `governance/BLUE_FIRST_VERTICAL_ONE_BIG_BUILD_FROZEN_SPEC_2026-09-21.md`.
Branch: `builder/post-p0-first-vertical-shadow-loop-2026-09-21`, HEAD `2bc80e4`
(descends from frozen `aa6a5c1`; CI 35628638457 = COMPLETED/SUCCESS at that exact
SHA; launch gate satisfied).

Done: vendored the 30 REUSE_AS_IS files listed in
`governance/BLUE_ONE_BIG_BUILD_IMPORT_MANIFEST_2026-09-21.md` §2-4 from
`parallel/claude-forward-data-2026-09-20@83521dbf...` (dataplane admissibility/
form4_parse/forward_admissibility/forward_recorder + full `science/` package) and
`parallel/claude-economic-v2-2026-09-20@35dff27b...` (full `economics/` package,
19 files). All 30 blob SHAs verified byte-identical to the manifest; all 30
modules import cleanly. Committed/pushed at `2bc80e4`.

Not yet done: `src/quant/science/effect.py` (assemble_form4_effect, net-new per
manifest §3), cohort protocol state, method qualification, `src/quant/integration/`
(forward_adapter.py, econ_bridge.py), removal of the auto VALIDATED->SHADOW
transition in `src/quant/factory/workers.py::_finish` (lines 241-246: two
back-to-back `.transition()` calls with no gate between), Desk/Risk/Fill/Book
wiring, Learning idempotence, tests, final handoff.

Key API facts recovered (to avoid re-deriving from scratch):
- `quant.economics.decision.economic_gate(estimate, meue_result, theta,
  interaction, capacity=None, consistency=None) -> EconomicVerdict` (CONTINUE/
  NO_TRADE/KILL; never raises). No single "run everything" function exists;
  intended chain is `MEUERecipe.evaluate(theta) -> MEUEResult -> economic_gate(...)
  -> compute_input_fingerprint(...) -> AssessmentRecord.from_verdict(...) ->
  EconomicAssessmentJournal.record(...)` (journal.py; raises `AssessmentConflict`
  on same id + different fingerprint, idempotent no-op on same id + same
  fingerprint).
- `quant.economics.coordinate.evaluate_delta_coordinate(binding) ->
  DeltaCoordinateVerdict` with distinct `DELTA_COORDINATE_UNRESOLVED` vs
  `_MISMATCH` states.
- No `economic_margin_notional` field exists anywhere; derive via
  `capital * MarginSizingRule.fraction(margin_of_safety)` or
  `quant.economics.sizing.size_lane(...)`.
- Pre-size cost check: `quant.economics.consistency.verify_research_cost_
  consistency(research_one_way_cost_bps, ExecutionCostModel.from_execution_model
  (desk.execution.ExecutionModel), participation_used, subject)`.
- `quant.economics.opening.OpeningExecutionModel.fill(...)` returns a plain dict,
  no persistence/idempotency key — confirmed modelling-only, must not be wired as
  a live Desk fill/booking path.
- `src/quant/factory/workers.py::_finish` (~line 217-257): auto-promotes
  RESEARCH->VALIDATED->SHADOW unconditionally on `verdict["passed"]`, no economic
  gate in between — this is the exact interception point for M2.
- `src/quant/desk/desk.py::CapitalDesk.actionable()` admits any strategy with
  `lifecycle in TRADABLE` or `evaluation_track=True` — no economic-gate check
  there either; economic admission must land before this selection, not inside it.
- `src/quant/book/ledger.py::Ledger.apply_fill(..., operation_id)` already has a
  true idempotent-replay guard (`operation_id in self._applied`) — reuse this
  pattern for any new idempotence surfaces (Learning processed-ids, Economic
  journal) rather than inventing a second mechanism.
- `src/quant/economics/`, `src/quant/science/`, `src/quant/integration/` did not
  exist on the pre-freeze Blue spine; `src/quant/learning/store.py`'s only true
  permanent-membership idempotent dict is `build_tasks` (its `lessons` list is
  truncated to last-200 on save, not a durable processed-id ledger).

Next action on resume: read `src/quant/science/{eligibility,formation,inference,
invariance,nulls,regimes}.py` and `src/quant/dataplane/{admissibility,form4_parse,
forward_admissibility,forward_recorder}.py` in full (not yet digested as of this
checkpoint) before writing `effect.py`, per
`governance/BLUE_RESEARCH_FROZEN_EFFECT_ESTIMATE_PRESTAGE_2026-09-21.md` §10
(exact frozen point estimator, D19 contract, DeltaCoordinateBinding field values
already recorded there).

### M1 design plan (not yet written) — `src/quant/science/effect.py`

Reuse confirmed (do not reinvent): `quant.economics.coordinate.{DeltaCoordinateBinding,
ReturnConvention, evaluate_delta_coordinate, ALLOCATION_WEIGHTED_RATIO}` (already
vendored, exact fields read in full) for the DeltaCoordinateBinding step —
`security`/`benchmark` are `ReturnConvention(return_definition="SIMPLE_SINGLE_PERIOD",
interval_spec="PUBLIC_KNOWLEDGE_NEXT_OPEN_TO_20TH_CLOSE_V1",
corporate_action_convention="SHARE_ENTITLEMENTS_PLUS_UNREINVESTED_CASH_V1",
terminal_treatment=...)`, `benchmark_symbol="SPY"`,
`aggregation=ALLOCATION_WEIGHTED_RATIO`, `allocation_constructor_id=
"FORM4_SLOT20_ADV20_V1"`, `allocation_weight_precedes_outcome=True`,
`d19_reference` set whenever `terminal_treatment` is set (else
`_missing()` flags `TERMINAL_TREATMENT_WITHOUT_D19_REFERENCE`).
Point/interval math: `quant.science.inference.ratio_estimate(weights, outcomes,
clusters=component_ids, confidence_level)` for delta_hat (NOT
`cluster_bootstrap_ratio` — that resamples clusters with replacement, not a wild/
Rademacher sign-flip). `quant.science.nulls.cluster_sign_flip_null` is the closest
existing sign-flip primitive but only produces a null distribution (not a CI) and
only supports RNG draws, not deterministic exhaustive `2^G` enumeration — write a
new `sign_flip_interval()` in effect.py: recenter each component's demeaned
contribution `sum_j a_j*(T_j-point)` per component, enumerate all `2^G` sign
vectors via `itertools.product((1,-1), repeat=G)` when G is small (cap ~20),
else fall back to a fixed-size seeded sample: `random.Random(seed)` picks over
`itertools.product`, take percentiles of `point + sum(sign*contribution)/denom`.

New (nothing upstream provides these — confirmed by two independent research
passes, safe to write without re-checking): 252-session/K<=4/80-gap cohort
geometry and gap enforcement (reuse only `quant.science.formation.SessionCalendar
.index_of/next_session` for session-index arithmetic — its `FormationEngine`
10-session/threshold-2 state machine is a *different*, unrelated D07 geometry,
do not repurpose it); union-find connected components across cohorts (issuer_cik
+ corporate predecessor/successor merge map); `FORM4_SLOT20_ADV20_V1` allocation
(`weight_j = min(C0/SLOT_COUNT, 0.001*ADV20_j)`, C0=$10,000, SLOT_COUNT=20, so
per-slot cap = $500); the structural one-look stopping decision (its function
signature must take NO outcomes/T_j parameter at all — that is how "no target
outcome read during structural stopping" gets proven, not by a runtime check);
G>=10 and max-component-share<=0.10 guards; D19 fail-closed to
`lower=-inf, upper=+inf` + reason `D19_TERMINAL_TREATMENT_UNRESOLVED_ESTIMATE_
REFUSED` (no infinite-interval refusal object exists upstream); a
`MethodQualificationArtifact` + `qualify_method()` running 10 deterministic
synthetic stress cases (null/positive/common-shock/repeated-issuer/overlapping-
window/unequal-allocation/random-denominator/one-dominant-component/insufficient-
component-count/dependence-misspecification), gating on `DEPENDENCE_MODEL_
UNSUPPORTED` when unqualified; `CohortProtocolStore` at the new paths.py
properties `science_cohort_protocol` / `science_method_qualification` (added,
committed) — first cohort activation freezes `protocol_hash =
recipe_hash(geometry_constants_dict)` (reuse `quant.economics.fingerprint.
recipe_hash`), later cohorts must match it or fail closed.

`assemble_form4_effect(...)` orchestration order: structural stop decision (no
outcomes touched) -> if CONTINUE, refuse "not yet eligible" (no artifact) -> if
K_MAX exhausted below guard, refuse `INSUFFICIENT_CLUSTER_INFORMATION` -> if
eligible: D19 check (fail closed first, before any point/interval math) ->
method qualification check (`DEPENDENCE_MODEL_UNSUPPORTED` if missing/unqualified)
-> build `DeltaCoordinateBinding`, `evaluate_delta_coordinate` (UNRESOLVED/
MISMATCH refuse distinctly, before outcomes are read) -> only now read
`outcomes: Mapping[event_id, float]` -> `ratio_estimate(...)` + `sign_flip_
interval(...)` -> `ScientificEffectArtifact` with `evidence_label=
FORWARD_CONFIRMATION` (guard+qualification both passed) or `DEVELOPMENT`
otherwise, carrying `protocol_hash`, `delta_coordinate_hash`, `component_
assignments`, `reason_codes`.

Priority test subset for M1 (out of the mission's 20): protocol-freeze
immutability, <80-session gap fail-closed, G<10-after-cohort-N continues
structurally, K_MAX exhausted+G<10 -> INSUFFICIENT_CLUSTER_INFORMATION,
concentration guard, D19 fail-closed before any estimate, missing/failed method
qualification -> DEPENDENCE_MODEL_UNSUPPORTED, coordinate UNRESOLVED/MISMATCH
refuse distinctly, structural-stopping function signature takes no outcomes
parameter (proves one-look by construction), deterministic sign-flip
reproducibility (same seed -> same interval).

## M2 — Research -> Economic admission boundary (2026-09-21)

`M2_RESEARCH_ECONOMIC_BOUNDARY = GREEN`

M1 (`src/quant/science/effect.py`, cohort protocol, method qualification) was
already done and green (396 tests) at the start of this checkpoint; M1's status
was never written up here, so it is folded into this entry for continuity. A
concurrent M1-hardening commit (`e470e3d`, "harden M1 one-look and cohort
maturation authority", rebased onto during this checkpoint) raised the base
to 401 tests, and also introduced a literal `\n`-escaped two-statement line in
`src/quant/science/effect.py` (`CONFIRMATORY_LOOK_ALREADY_CONSUMED = "..."\n
CONFIRMATORY_LOOK_AUTHORITY_MISSING = "..."\n` as one physical source line)
that made the module fail to import (`SyntaxError: unexpected character after
line continuation character`). Fixed as part of this checkpoint's rebase
(split into two real lines; no semantic change) since it blocked the whole
suite, not just M2.

Implemented:

- `src/quant/integration/__init__.py`, `src/quant/integration/forward_adapter.py`,
  `src/quant/integration/econ_bridge.py` — the new, sole owner of any
  `VALIDATED -> SHADOW` transition. Nothing else in the tree performs that
  transition any more.
- Removed the automatic `VALIDATED -> SHADOW` transition from
  `src/quant/factory/workers.py::_finish` (the two back-to-back
  `.transition()` calls). A passing lane now only reaches `VALIDATED`;
  `evaluation_track=True` on a rejected lane is unchanged (pre-existing
  behaviour, out of M2 scope).
- `forward_adapter.ForwardEvidenceIdentity` scopes the durable assessment
  identity to exactly the evidence a
  `quant.science.effect.ScientificEffectArtifact` consumed: `sample_provenance`,
  `protocol_id`/`protocol_hash`, `sample_id`/`cohort_ids`, the *sorted*
  `allocation_commitment_hashes` (exact contributing content addresses),
  `delta_coordinate_hash`, the subject (`ticket_id:strategy_id`), and — from an
  explicit `ForwardConfirmationReceipt`, never a caller-supplied boolean — the
  session seal and UseLedger identity. None of these fields can change when
  unrelated Forward evidence arrives elsewhere (`test_forward_evidence_identity_
  unaffected_by_unrelated_ledger_growth`); `derive_forward_evidence_identity`
  has no `forward_confirmed` parameter at all (`test_forward_adapter_never_
  accepts_a_boolean_confirmation_flag` asserts the `TypeError`).
- `econ_bridge.assess_and_admit` runs the frozen chain: scientific effect
  (`ScientificEffectArtifact.effect_estimate`) -> `economic_gate(...)` ->
  `compute_input_fingerprint(...)` -> `AssessmentRecord.from_verdict(...)` ->
  `EconomicAssessmentJournal.record(...)` (new durable path,
  `QuantPaths.economic_assessments`) -> `VALIDATED -> SHADOW` iff
  `verdict.verdict == CONTINUE` and `capital_order_eligibility ==
  PORTFOLIO_CONSIDERATION_ELIGIBLE`. Every other outcome leaves the strategy
  exactly where it was; no fill, no Book mutation.
- Three distinct refusal reasons, read straight through from already-existing
  constants rather than invented fresh:
  `quant.science.effect.EFFECT_UNAVAILABLE`
  (`EFFECT_ESTIMATE_ON_FROZEN_COORDINATE_UNAVAILABLE`),
  `quant.economics.states.DELTA_COORDINATE_UNRESOLVED`,
  `quant.economics.states.DELTA_COORDINATE_MISMATCH`. None of the three, and
  no `economic_gate` verdict reason, is ever collapsed to a generic
  `"NO_TRADE"` string by the bridge — the verdict's own reason (e.g.
  `ECONOMICALLY_DOMINATED_ACROSS_WHOLE_INTERVAL`,
  `UNCERTAINTY_STRADDLES_ECONOMIC_THRESHOLD`) is what gets recorded.
- Idempotent replay / conflict reuse `EconomicAssessmentJournal` exactly as
  M1's design plan recorded it: same `assessment_id` + same
  `input_fingerprint` -> no-op replay, no second lifecycle mutation; same
  `assessment_id` + different fingerprint -> `AssessmentConflict` propagates
  out of `assess_and_admit` uncaught (fail closed, prior record untouched).
- Verified rather than assumed (per mission instruction):
  `quant.desk.desk.CapitalDesk.actionable()` selects on
  `definition.tradable or definition.evaluation_track`; a bare `VALIDATED`
  strategy is neither, so it was already excluded before this checkpoint and
  remains excluded now that nothing auto-promotes it. No Desk/Risk/Execution/
  Book/Learning file was modified.
- `src/quant/paths.py` gained one new property,
  `QuantPaths.economic_assessments` (`var/economic_assessments.jsonl`), for
  the journal above. No other paths changed.

Tests: `tests/test_m2_research_economic_boundary.py` (15 new tests) — research
VALIDATED alone never Desk-actionable; a full research run
(`test_v1_red_team.register_fixture`/`research_system`/`run_research_only`)
never leaves a strategy at `SHADOW`; `NO_TRADE`/`KILL`/`CONTINUE`-but-ineligible
each leave the strategy at `VALIDATED`; `CONTINUE`+eligible promotes exactly
once (checked both by `lifecycle_history` length and by replay producing
`promoted_to_shadow=False` the second time); replay is a journal no-op
(`len(journal) == 1` after two identical calls); same id + changed `theta`
conflicts and fails closed; missing effect estimate, `DELTA_COORDINATE_
UNRESOLVED` and `DELTA_COORDINATE_MISMATCH` each report their own distinct
reason; an already-`SHADOW` strategy is never re-promoted; forward evidence
identity is unaffected by reordering/regenerating an unrelated field.

Full suite: **416 unit tests, all green**
(`PYTHONPATH=src python3 -m unittest discover -s tests -v`).
`python3 -m compileall src` and `python3 scripts/generate_schemas.py --check`
both pass. `STATE.md`'s proof-inventory line was updated 401 -> 416 (401 from
the rebased-onto M1-hardening commit, +15 from this checkpoint's M2 tests) to
keep `test_astra_pre_t0.py::test_published_test_inventory_matches_
authoritative_package_discovery` green (it cross-checks this file's declared
count against live package discovery).

Not covered by M2, intentionally deferred to M3 per the frozen mission
boundary: `MEUERecipe` assembly / `ThetaState` construction for a real
strategy (the bridge takes an already-evaluated `MEUEResult`/`ThetaState` as
input — assembling those from live desk/economic parameters is M3's Economic
`SIZE` work, not the admission boundary itself); wiring `assemble_form4_effect`
into a live Form4 cohort pipeline for the existing ETF cross-sectional lanes
(those lanes carry no Form4 cohort evidence today, so every real VALIDATED
strategy from `factory/workers.py` will correctly refuse at the boundary with
`EFFECT_ESTIMATE_ON_FROZEN_COORDINATE_UNAVAILABLE` until a Form4-evidenced
strategy exists — this is the intended fail-closed behaviour, not a gap); a
Control-Plane/Clock call site that invokes `assess_and_admit` automatically
between research completion and a Desk session (today it is a library call
the next milestone, or an operator, invokes explicitly).

Next action for M3 (`M3_DESK_BOOK_BOUNDARY`): implement the Economic
`MarginSizingRule`/lifecycle cap (`FINAL_SIZE = min(economic_margin_notional,
desk_lifecycle_cap_notional)`), independent Risk veto/throttle, wire
`ExecutionModel.fill`'s entry/scheduled-exit phases as the sole Book-feeding
authority, both mandatory cost-consistency checks (pre-size model envelope;
post-size/pre-fill actual implied participation), and exact provenance/
operation identity so replay cannot double-apply a fill. Read
`src/quant/desk/{desk,risk,execution}.py` and `src/quant/book/ledger.py` in
full before writing M3 code — not yet digested as of this checkpoint.

## M3 — Desk/Book execution boundary: SIZE -> RISK -> FILLS -> BOOK (2026-09-21)

`M3_DESK_BOOK_BOUNDARY = GREEN`

Ported the M3 draft prepared ahead of M2 landing (in the sibling worktree
`.claude/worktrees/agent-afb10da05a4e66135`, based on pre-M2 state) onto this
branch's M2 HEAD, resolving both interface gaps the draft author flagged
before wiring it into `desk.py`:

- `src/quant/desk/economic_size.py` (new) — the standalone
  SIZE -> pre-size cost check -> independent Risk -> post-size cost check ->
  FILLS -> BOOK authority (`run_lane_entry`/`run_lane_scheduled_exit`),
  calling the existing, unmodified `desk.risk`, `desk.execution.ExecutionModel`
  and `book.ledger.Ledger` -- no second Book, execution engine or ticket
  family.
- `src/quant/desk/execution.py` -- `ExecutionModel.fill` gained an explicit
  `phase` parameter (`ENTRY` / `SCHEDULED_EXIT`, same model and cost
  mechanics for both); an unrecognised phase raises `UnknownExecutionPhase`
  rather than defaulting silently.
- `src/quant/economics/consistency.py` -- added the mandatory post-size/
  pre-fill execution-cost consistency check (`AdvReference`,
  `verify_post_size_participation_consistency`), re-run on the *actual* final
  sized notional against the authorized ADV/capacity input, downstream of the
  existing pre-size check.
- `src/quant/economics/sizing.py` -- added `compute_final_size`/
  `LaneSizeResult`: `FINAL_SIZE = min(economic_margin_notional,
  desk_lifecycle_cap_notional)`; the lifecycle capital fraction can only ever
  reduce size, never manufacture it, because `MarginSizingRule.fraction` is
  clamped to `[0, max_fraction]`.

**Gap 1 (`margin_of_safety` not durably persisted) -- resolved by extending
`AssessmentRecord`, not by inventing a parallel parameter.**
`src/quant/economics/journal.py::AssessmentRecord` gained two fields:
`strategy_id: str = ""` and `margin_of_safety: float | None = None`.
`AssessmentRecord.from_verdict` now takes `strategy_id` and populates
`margin_of_safety` directly from `EconomicVerdict.margin_of_safety` -- the
exact number `econ_bridge.assess_and_admit` already computes at its
SHADOW-promotion call site (both the accepted and `ADMISSION_REFUSED` record
paths in `econ_bridge.py` now pass `strategy_id`). Added
`EconomicAssessmentJournal.latest_for_strategy(strategy_id)` (most recent
durable record by `recorded_at`) since records are keyed by content-addressed
`assessment_id`, not by strategy. `desk.economic_size.LaneEconomicAdmission.
from_assessment_record(record, margin_rule)` builds the admission M3 SIZE
needs from a real durable record; a record with no `margin_of_safety` (e.g.
one persisted before this checkpoint) surfaces as `0.0`, never a fabricated
positive margin.

**Gap 2 (Ledger idempotence keyed on `operation_id` alone; same id +
conflicting fill was a silent no-op) -- resolved with a narrow, additive fix
to `book/ledger.py` only, mirroring `economics.journal.AssessmentConflict`.**
`LedgerState` gained `operation_fingerprints: dict[str, str]`; `apply_fill`
computes a semantic fingerprint (`sha256` of
symbol/quantity/price/cost/date/strategy_id) and, on replay under an id
already applied, compares it: matching fingerprint stays the existing
idempotent no-op, mismatched fingerprint raises the new `FillConflict`
(`book/ledger.py`) before any mutation, rather than silently reusing the id.
Satisfies frozen-spec acceptance case #26/#54. No other `Ledger` behaviour
touched.

**The real splice, not just the standalone module.**
`src/quant/desk/desk.py::CapitalDesk`:

- `__init__` gained `assessment_journal`/`margin_rule` (both default-
  constructed: `EconomicAssessmentJournal(paths.economic_assessments)`, a
  `DESK_DEFAULT_MARGIN_RULE` with `reference_margin=0.05, max_fraction=1.0`).
- New `_economic_admission(definition)` reads
  `assessment_journal.latest_for_strategy` and builds a
  `LaneEconomicAdmission` via `from_assessment_record` -- never re-derives
  `margin_of_safety` and never calls `economic_gate` directly.
- The SIZE stage's naive `capital = nav * capital_fraction * allocation` line
  now calls `economic_size.compute_final_size` whenever a durable,
  capital-order-eligible `AssessmentRecord` exists for the strategy, with
  `definition.capital_fraction` supplied as the lifecycle cap (unchanged
  ceiling semantics). A strategy with **no** such record (every existing
  test/demo strategy, which is constructed directly without ever calling
  `econ_bridge.assess_and_admit`) keeps the pre-M3 naive sizing byte-for-byte
  unchanged -- this is why the full suite stayed green with zero test
  rewrites outside the new M3 file.
- New `_maybe_scheduled_exit` hook: today's SCAN stage returned `NO_TRADE`
  the instant a session produced no qualifying signal, even with an open
  sleeve -- there was no explicit exit phase at all. The hook fires once per
  strategy per cycle, only when the sleeve is open, `holding_days` has
  elapsed (reusing the existing `_sessions_since_rebalance` signal, not a new
  scheduler) and a durable admission exists, and flattens the sleeve through
  `economic_size.run_lane_scheduled_exit` (`phase=SCHEDULED_EXIT`) -- the
  same authoritative chain, never a second one. ADV references for the exit
  are read from `ExecutionModel.adv()` (already-existing method) rather than
  a new capacity component.

Tests: ported `tests/test_m3_size_risk_fills_book.py` (34 tests, up from the
draft's original set): standalone `economic_size` entry/scheduled-exit/sizing
tests unchanged in substance; `test_same_operation_id_conflicting_fill_
semantics_is_a_conflict` now asserts `FillConflict` is raised (previously
pinned the gap as a silent replay -- inverted once gap 2 was fixed), plus a
same-fingerprint idempotent-replay test and a cross-strategy-conflict test;
new `MarginOfSafetyThreadingTests` (gap 1, `AssessmentRecord.from_verdict` ->
`EconomicAssessmentJournal.latest_for_strategy` ->
`LaneEconomicAdmission.from_assessment_record`, including the no-margin ->
`0.0` fallback); new `DeskSpliceTests` (the real `CapitalDesk` SIZE-stage
splice end to end, both the economic-sizing branch with a durable admission
wired via `EconomicAssessmentJournal` and the naive-fallback branch with
none); new `ScheduledExitHookTests` (`_maybe_scheduled_exit` flattens an open
sleeve past its holding period, and is a no-op with no open sleeve).

Full suite: **450 unit tests, all green**
(`PYTHONPATH=src python3 -m unittest discover -s tests -v`; up from 416, all
34 in the new M3 file). `python3 -m compileall src`,
`python3 scripts/generate_schemas.py --check` (after `book_state.schema.json`
was regenerated for the new `operation_fingerprints` field) and
`python3 scripts/demo_quant_system.py` all pass.
`test_astra_pre_t0.py::test_published_test_inventory_matches_authoritative_
package_discovery` required this file's proof-inventory line updated
416 -> 450 to stay green.

Not covered by this checkpoint, intentionally deferred: a live Control-Plane/
Clock call site that runs `assess_and_admit` automatically ahead of a Desk
session remains a library call an operator invokes explicitly (unchanged
from M2); the scheduled-exit hook's ADV references come from
`ExecutionModel.adv()` rather than a dedicated capacity/liquidity component
(none exists yet in this tree); the M4 draft in
`.claude/worktrees/agent-a1f3b1232a841ab76` was not touched, per the mission
boundary -- a separate reconciliation pass handles it once M3 has landed.

## M4 — durable Learning processed-id/payload-digest authority, wired end to end (2026-09-21)

`M4_LEARNING_E2E = GREEN`

Ported the M4 draft prepared ahead of M2/M3 landing (in the sibling worktree
`.claude/worktrees/agent-a1f3b1232a841ab76`, based on pre-M2/M3 state) onto
this branch's M3 HEAD, then wired it into the real call sites the draft could
not yet see:

- `src/quant/learning/durable.py` (ported unchanged except one addition: a
  `KILL` kind, since `economic_gate`'s `KILL` verdict is a real M2 outcome
  the draft predates) — `DurableOutcomeStore.record(processed_id, kind,
  payload, links=None)`: an unseen `processed_id` is durably recorded
  (`RECORDED`); a replay with an identical payload digest is a `NOOP`; a
  replay with a different digest raises `LearningConflict` and leaves the
  store byte-for-byte unchanged. Separate from `LearningStore.lessons`
  (capped to the last 200 on save), so replaying an old id after 200 later
  entries still finds it.
- `src/quant/paths.py` — `QuantPaths.learning_outcomes`
  (`var/learning_outcomes.json`).
- `src/quant/learning/__init__.py` — re-exports.
- `src/quant/integration/econ_bridge.py::assess_and_admit` gained an optional
  `learning=` parameter. Every terminal branch durably records exactly one
  outcome, keyed by the same identity the boundary already computed
  (`assessment_id` for a resolved evaluation, the refusal identity seed for
  one that never resolved), so a replay of the same evidence is a Learning
  `NOOP` exactly when `journal.record` is also a no-op: refusal-before-
  identity with `STRUCTURAL_INSUFFICIENT` in the artifact's reason codes ->
  `INSUFFICIENT`; any other refusal-before-identity (missing estimate,
  `DELTA_COORDINATE_UNRESOLVED`/`_MISMATCH`) -> `ECONOMIC_ASSESSMENT`;
  resolved `NO_TRADE` -> `NO_TRADE`; resolved `KILL` -> `KILL`; resolved
  `CONTINUE` (eligible or development-signal-only) -> `ECONOMIC_ASSESSMENT`.
- `src/quant/desk/economic_size.py::run_lane_entry`/`run_lane_scheduled_exit`
  gained an optional `learning=` parameter. New `_terminal()`/
  `_record_lane_outcome()` helpers record exactly one outcome per terminal
  `LaneDecisionCard` (`OUTCOME_NO_TRADE` -> `NO_TRADE`, `OUTCOME_VETOED` ->
  `RISK_VETO`, `OUTCOME_BOOKED` -> `BOOKED`), keyed by
  `opportunity_id:phase:action` -- deterministic, so a crash-and-replay of
  the same lane run with the same `opportunity_id` lands a `NOOP`, matching
  `Ledger.apply_fill`'s own `operation_id`-keyed replay guard on the BOOKED
  path. The upstream science outcome (`STRUCTURAL_INSUFFICIENT` /
  `INSUFFICIENT_CLUSTER_INFORMATION` in `quant.science.effect`) is recorded
  upstream at `econ_bridge.assess_and_admit`, confirmed against the real M1-M3
  code: a Desk `LaneDecisionCard`/`OpportunityTicket` never itself reaches a
  literal `INSUFFICIENT` action, so `economic_size.py` only ever emits
  `NO_TRADE`/`RISK_VETO`/`BOOKED`.
- `src/quant/desk/desk.py::CapitalDesk` gained an optional `learning=`
  parameter, threaded to the existing `run_lane_scheduled_exit` call site
  (the one M3 splice into `desk.py`; `_run_strategy`'s inline entry path is
  unchanged and does not yet call `run_lane_entry` -- see "not covered"
  below, the same deferral already on record for M3).
- `src/quant/clock.py::QuantSystem` constructs one `DurableOutcomeStore`
  (`self.learning_outcomes`), shares it with `self.desk`, and records a
  `RESEARCH_RESULT` outcome keyed by `ticket_id` whenever
  `factory.workers.run_lane`'s result outcome is `VALIDATED` — additive to
  the existing `LearningStore.record_research` call, never a replacement.

**INSUFFICIENT ambiguity, resolved and confirmed against real code.** The
draft's note ("`INSUFFICIENT` in the mission's M4 test list read as the
upstream science outcome, not a desk `OpportunityTicket` status") was
verified against the real M2/M3 vocabulary: `economic_size.py`'s three
terminal actions are exactly `NO_TRADE`/`VETOED`/`BOOKED`; the only place
`STRUCTURAL_INSUFFICIENT` (`INSUFFICIENT_CLUSTER_INFORMATION`) can reach the
economic boundary is via a refused-before-identity `ScientificEffectArtifact`
that declares it in its own `reason_codes`, at `econ_bridge.assess_and_admit`.
The reading holds; no new state was invented anywhere downstream.

Tests: ported `tests/test_learning_durable.py`'s 10 draft tests (still exercise
`DurableOutcomeStore` directly, useful as fast synthetic coverage), plus 7 new
`RealEndToEndWiringTests` that exercise the actual `assess_and_admit`/
`run_lane_entry` call sites with `learning=` supplied: NO_TRADE, KILL,
INSUFFICIENT-refusal (plus its replay), CONTINUE+eligible, Risk VETO, BOOKED
(plus a crash-and-retry replay proving one fill and one durable record, not
two), and a true end-to-end restart/replay across 200+ later entries. New
`tests/test_vertical_e2e.py` covers the mission's full E2E list: the negative
path (no scientific evidence -> fail closed, zero Book mutation, asserted
directly on `Ledger.state.fills`/`cash`/`applied_operations`); a labeled
`SYNTHETIC_POSITIVE_PLUMBING_FIXTURE` walking CONTINUE -> SHADOW -> SIZE ->
Risk -> one real fill -> one real Book mutation -> exactly two durable
Learning records; Risk-veto, zero-size and post-size-cost-failure E2E paths;
`SoleAuthorityAndIsolationTests` (AST-level source search proving exactly one
`Ledger`/`ExecutionModel`/`CapitalDesk`/`DurableOutcomeStore` class exists
under `src/quant`, and that none of the M2-M4 boundary modules import a
network client).

Full suite: **478 unit tests, all green**
(`PYTHONPATH=src python3 -m unittest discover -s tests -v`; up from 450, all
28 new in `test_learning_durable.py`/`test_vertical_e2e.py`). `python3 -m
compileall src`, `python3 scripts/generate_schemas.py --check` and `python3
scripts/demo_quant_system.py` all pass.
`test_astra_pre_t0.py::test_published_test_inventory_matches_authoritative_
package_discovery` required this file's proof-inventory line updated
450 -> 478 (regenerated via `python3 scripts/status_artifacts.py --write`) to
stay green.

Not covered by this checkpoint, intentionally deferred (see
`handoff/BUILDER_POST_P0_FIRST_VERTICAL_SHADOW_LOOP_2026-09-21.md`'s
`KNOWN_DEFERRED_LIMITATIONS` for the full statement): `CapitalDesk.
_run_strategy`'s inline entry path still does not call `economic_size.
run_lane_entry`, so a real Desk session's live entry outcome is not yet
durably recorded (only the already-spliced scheduled-exit path is); no
Control-Plane/Clock call site invokes `assess_and_admit` automatically ahead
of a Desk session (unchanged from M2/M3); `REAL_DATA_POSITIVE_PATH_AVAILABLE`
is `FALSE` per the frozen science geometry's own multi-year structural
timeline, not a gap this checkpoint could close.

This is the final milestone of the mission.
`BUILDER_VERTICAL_SHADOW_LOOP = READY_FOR_INDEPENDENT_REVIEW`.

## Human boundary currently reached?

No. Nothing in the current frontier requires human authority. The blocked lanes need datasets
that are free but not yet written adapters for; the next research lane is choosable from
evidence. The system is `IDLE` with a named next action, not finished.


## Claude resume freeze checkpoint — 2026-09-21

User-requested stop point. No further Product/science implementation was performed after
commit `be5177fc8711c6aedb8e75cf61a9eb3243492362`.

Resume contract for the next Claude Code Builder:

- stay on `builder/post-p0-first-vertical-shadow-loop-2026-09-21`; do not create another branch;
- treat `be5177f` and the checkpoint immediately above as the complete durable work state;
- do not redo selective imports or digest verification;
- the durable M1 paths `QuantPaths.science_cohort_protocol` and
  `QuantPaths.science_method_qualification` are already committed;
- the detailed `src/quant/science/effect.py` design/reuse plan immediately above is the direct
  implementation starting point;
- continue the frozen one-big-build mission from M1, beginning with `effect.py` and its targeted
  deterministic tests;
- preserve all Blue frozen authorities and mission prohibitions;
- no target-host, P0, Gate-B, real-network, or real-capital mutation is authorized by this
  checkpoint.

CHECKPOINT_KIND = CLAUDE_RESUME_ONLY
SEMANTIC_IMPLEMENTATION_AFTER_BE5177F = FALSE
WORKTREE_STATE_AT_HANDOFF = REMOTE_DURABLE
