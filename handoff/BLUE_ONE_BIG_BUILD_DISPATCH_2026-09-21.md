# BLUE — ONE BIG BUILD DISPATCH AUTHORITY — 2026-09-21

## 0. Preconditions verified

Frozen build specification:

`governance/BLUE_FIRST_VERTICAL_ONE_BIG_BUILD_FROZEN_SPEC_2026-09-21.md`

Frozen-spec commit:

`aa6a5c17467d0da17482b88ab1dfcc362ec24d46`

Exact-head CI:

`35628638457 = COMPLETED / SUCCESS`

Current authorized Builder branch:

`builder/post-p0-first-vertical-shadow-loop-2026-09-21`

At dispatch reception the branch exists and resolves to the exact frozen base:

`aa6a5c17467d0da17482b88ab1dfcc362ec24d46`

Selective import manifest:

`governance/BLUE_ONE_BIG_BUILD_IMPORT_MANIFEST_2026-09-21.md`

Cohort geometry:

`COHORT_GEOMETRY = FROZEN`

Science:

`SCIENCE_SPEC_STATE = CLOSED_FOR_IMPLEMENTATION`

## 1. Blue authorization

Blue now explicitly authorizes ONE isolated Product implementation mission:

```text
ONE_BIG_BUILD = AUTHORIZED
PRODUCT_IMPLEMENTATION_AUTHORIZED = TRUE_FOR_ASSIGNED_BUILDER_ONLY
AUTHORIZED_BUILDER_BRANCH =
  builder/post-p0-first-vertical-shadow-loop-2026-09-21
AUTHORIZED_BUILD_BASE =
  aa6a5c17467d0da17482b88ab1dfcc362ec24d46
VERTICAL_LOOP_BUILD_SPEC = FROZEN
```

This authorization does NOT authorize integration, deployment, target-host changes or
capital.

```text
PRODUCT_INTEGRATION = PAUSED
TARGET_HOST_TOUCHED = FALSE
GATE_B_MUTATION_AUTHORIZED = FALSE
GATE_B_PASS_DECLARED = FALSE
t0 = NOT_DECLARED
REAL_CAPITAL_AUTHORIZED = FALSE
```

## 2. Scope

The Builder implements exactly the frozen first traceable economic loop:

`Forward -> Research -> Scientific Effect -> Economic -> SHADOW -> SIZE -> RISK -> FILLS -> BOOK -> Learning`

Internal milestones:

```text
M1_SCIENTIFIC_ARTIFACT
M2_RESEARCH_ECONOMIC_BOUNDARY
M3_DESK_BOOK_BOUNDARY
M4_LEARNING_E2E
```

They are one Builder mission, not separate agents.

## 3. Anti-drift

The Builder may not reopen settled architecture/science merely for preference.

If actual code creates a real contradiction, it must raise one precise:

`BLUE_DECISION_REQUIRED_<ID>`

and preserve all completed work.

The Builder must not self-certify Product PASS.

Final allowed Builder disposition:

`BUILDER_VERTICAL_SHADOW_LOOP = READY_FOR_INDEPENDENT_REVIEW`

or one precise blocker.

## 4. Exit

```text
ECONOMIC_PROGRESS = implementation of the first traceable economic loop is authorized
REMAINING_BLOCKER = Builder implementation + exact-head CI
EXIT_CONDITION = one durable Builder delivery ready for independent review
```
