# Astra independent pre-t0 falsification record

Authority: fresh GitHub clone, initially observed `3d4cb271a646400ff06c829155f339910492ef5c` (already ahead of the handoff's `8d5dbb41`). No previous local/uncommitted correction was used. PR #17 is not merged; t0 is NOT DECLARED; continuity remains OPEN / NOT_YET_PROVEN_CONTINUOUS.

This is an intermediate engineering record, not a Blue handoff or final rodage.

## Reproducible red/green phase 1

Red tests are committed at `35731abb479677888f4c2f4638b2cedf23f08343`. Run `PYTHONPATH=src python3 -m unittest tests.test_astra_pre_t0 -v`. Before corrections: 8 failures, 1 evidence-corruption error; 4 other checks passed. Each test below now passes locally; CI and deployment are separate gates.

| Stable defect id | Classification | Scenario / discriminating test | Minimal correction | Status |
|---|---|---|---|---|
| FUTURE_DUE_MISCLASSIFIED_AS_FUTURE_EVIDENCE | BLOCKS_CAPTURE_INTEGRITY | `test_future_due_is_commitment_not_future_evidence`; existing quiet-service tests also fail on the initial remote | Allow future due times; keep future observed times invalid | Corrected, local discriminant green |
| NULL_DUE_ERASES_EXPECTED_WORK | BLOCKS_CAPTURE_INTEGRITY | `test_missing_due_cannot_erase_all_expected_work`: AWAITING_POLL with no due/identity returns accountable=true | Require due for active states; disabled/config-failed transitions invalidate | Corrected, local discriminant green |
| CYCLIC_SUPERSESSION_FALSE_PASS | BLOCKS_CAPTURE_INTEGRITY | `test_two_node_circular_supersession_same_timestamp`: A replaces B and B replaces A at equal timestamps, both pass | Supersession may target only an earlier journal obligation | Corrected, local discriminant green |
| MISSING_OBSERVED_TIME_NOT_VALIDATED | BLOCKS_PIT_RECONSTRUCTABILITY | `test_missing_transition_time_is_invalid`: absent transition time produces no structural finding | Require observed timestamps independently of whether resolution happens | Corrected, local discriminant green |
| INFINITE_GRACE_FALSE_PASS | BLOCKS_CAPTURE_INTEGRITY | `test_nan_tolerance_cannot_make_obligations_pending`: infinite tolerance makes missed work pending forever | Require finite nonnegative grace | Corrected, local discriminant green |
| CORRUPT_EVIDENCE_NO_AUDIT_VERDICT | BLOCKS_CAPTURE_INTEGRITY | `test_corrupt_journal_returns_explicit_failed_verdict`: torn lifecycle tail raises instead of returning failed verdict | Sanitized explicit fail-closed audit result | Corrected, local discriminant green |
| MATERIALIZATION_MISMATCH_NOT_DURABLE | BLOCKS_CAPTURE_INTEGRITY | `test_fingerprint_mismatch_cannot_be_cleared_by_restoring_file`: mismatch, restore file, service accepts again | Create-once durable integrity fault; service/materializer/readiness refuse it | Corrected, local discriminant green |
| RESTART_REWRITES_OPEN_OBLIGATION | BLOCKS_CAPTURE_INTEGRITY | `test_restart_does_not_replace_the_unanswered_obligation`: real service-start method replaces durable commitment | Lifecycle append preserves existing obligation | Corrected, local discriminant green |
| SOCKET_TRICKLE_EXCEEDS_TOTAL_DEADLINE | BLOCKS_CAPTURE_INTEGRITY | `test_trickling_body_respects_total_deadline_real_socket`: real TCP body trickles beyond 0.12s deadline | Process deadline covers main-thread DNS/request/headers/body; read1 preserves chunks and bounds buffered reads | Corrected, local discriminant green |

All these code changes alter the acquisition fingerprint. All historical rodages are superseded for current-code acceptance; a new exact-head rodage is required. No 14-day evidence is claimed.

## Checks that did not reproduce a current defect

`test_real_main_child_exit_relaunch_keeps_host_identity` executes main, real subprocess child, durable state, then a second main invocation: kernel boot id stays distinct and both replacement starts are MANUAL_START. The original Blue defect exists at `8d5dbb41`; its remote correction is not inherited as a certification of the rest of lifecycle.

`test_due_poll_wins_over_a_backlog_in_real_clock` traverses the real clock capture step: due discovery beats queued drain. `test_real_connection_truncation_and_send_count` traverses http.client and a local TCP server: one request, exact partial bytes, incomplete outcome.

`test_single_foreign_fingerprint_cannot_pass` initially passed because future-due validation incidentally blocked it. This is NOT a separately reproduced defect yet; the implementation comparison was strengthened, and a discriminating mutation check remains necessary.

## Source-policy verification

On 2026-09-19 the official SEC developer and EDGAR-access pages support Latest Filings ownership filters, declared requester/contact User-Agent, and a total ceiling of 10 requests/second across machines. Current policy is more conservative. No external SEC requests are generated by this offline campaign.

- https://www.sec.gov/about/developer-resources
- https://www.sec.gov/search-filings/edgar-search-assistance/accessing-edgar-data
- https://docs.python.org/3/library/http.client.html

## Remaining campaign

Durable launch/exit authority binding; real signals, supervisor death and replacement; effective loaded unit; full request/budget/event ordering; state/journal crash boundaries; filing identity/package validation; discovery-page loss and reconciliation hot loops; global single-writer scope; public snapshot/event/history proxies; exact commit/tree/runtime evidence; actual target-host deployment and rodage. None is declared closed by this intermediate record.
