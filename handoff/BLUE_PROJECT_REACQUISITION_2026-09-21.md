# BLUE — PROJECT REACQUISITION / CURRENT FRONTIER — 2026-09-21

## 0. Purpose and precedence

This is the current Blue reacquisition checkpoint for a fresh conversation.

It is a routing/current-state authority, not a replacement for:
1. `QUANT_NORTH_STAR.md`;
2. exact governance/specification files it cites;
3. exact SHA/CI evidence.

When older routing/checkpoint language conflicts with this file, this file is the
latest Blue routing override until superseded by a later committed Blue checkpoint.

Always re-resolve live branch HEADs and CI before taking a state-changing action.

## 1. North Star alignment

Terminal objective remains:

`long-run real wealth growth after real frictions`.

Architectural target remains one persistent system:

`Data -> Research -> Opportunity -> VET -> SIZE -> RISK -> FILLS -> BOOK -> Learning`

with:
- persistent state;
- point-in-time provenance;
- no silent evidence relabeling;
- one authoritative Book;
- one authoritative execution path;
- `NO_TRADE` as a valid economically optimal outcome;
- wide/cheap monitoring before narrow/deep reasoning;
- Build Plane separated from runtime authority.

No gate, CI, test count, agent count or code volume is itself the terminal objective.

## 2. Current global safety state

```text
GATE_A_V4_REPOSITORY_DISPOSITION = PASS
P0_CONTINUITY_QUALIFICATION = HYBRID_EVENT_BASED_V1

TARGET_HOST_READY = FALSE
GATE_B_MUTATION_AUTHORIZED = FALSE
GATE_B = NOT_STARTED
GATE_B_PASS_DECLARED = FALSE

t0 = NOT_DECLARED

PRODUCT_INTEGRATION = PAUSED
REAL_CAPITAL_AUTHORIZED = FALSE
```

Frozen V4 candidate remains:

`4d06bdbf8ed008af5c9d4ab9469e61f34c3ba072`

Git tree:

`4d15ef6f471213ee6ab56337b555d2906ef9bf16`

Verified input-tree digest:

`sha256:ceaa2a1801e96a51dc8c86ea087a3a113d15aa757fbed0b7415129c157c231f2`

Selected V4 exact-head CI:

`35536353538 = COMPLETED / SUCCESS`

Artifact id:

`10612758620`

Artifact archive digest:

`sha256:06c690203d589e389847cd345809480da2b99254fe7fd7f38b82c6bf5d54659a`

## 3. Active project shape — TWO RAILS

Current substantial work is intentionally split into exactly two primary rails.

### RAIL A — P0 / Gate-B critical path

Purpose:

close the final repository F11 authority repair, receive independent Astra evidence,
then continue into the already-prepared Gate-B activation path.

### RAIL B — Product / Economic pre-big-build path

Purpose:

finish the scientific/economic contract and freeze one coherent vertical-loop build
specification before issuing one large bounded Product implementation mission.

Do not create several competing Product Builders.

The current strategy is:

`prepare -> adversarially challenge -> consolidate -> close science contract -> freeze -> ONE BIG BUILD`

## 4. Rail A — exact F11 state

Original integrated run-authority baseline:

`blue/gate-b-run-authority-repair-integration-2026-09-21@644da76eb0227be275b8e3448118dac0cc7096ca`

Original independent Astra finding:

`astra/gate-b-run-authority-mechanisms-recheck-2026-09-21@41c3f291f46b7b5849bdb702c09ccefbeecde691`

Finding:

`ASTRA_GATE_B_RUN_AUTHORITY_REVIEW = BLOCKED_REAL_DEFECT_LOCK_PATH_IDENTITY`

### Final Builder delivery

Branch:

`builder/gate-b-lock-path-identity-repair-2026-09-21`

Final Builder HEAD:

`e600295b2aa7056e8176e0286f9d67f5c65b1c11`

Final Builder exact-head CI:

`35617257622 = COMPLETED / SUCCESS`

Important implementation/code HEAD immediately before final handoff-only update:

`54a7dd6ebf8e81d6dec06bd82d150dd7faef44e4`

Its exact-head CI:

`35615746802 = COMPLETED / SUCCESS`

The final `e600295b...` commit is handoff-only relative to `54a7dd6e...`.

Builder reports repository-local closure of:

```text
F11_PUBLIC_LOCK_REPLACEMENT = GREEN
F11_LOCK_PLUS_ANCHOR_REPLACEMENT = GREEN
F11_P1_WHOLE_PARENT_REPLACEMENT = GREEN
F11_P2_REJECTED_PARENT_NO_MUTATION = GREEN
F11_P3_SINGLE_HISTORY_RECOVERY = GREEN
F11_NORMAL_MULTIPROCESS_SERIALIZATION = GREEN
```

Key new implementation mechanism:
- critical-section name resolution anchored to held parent-directory fd;
- grandparent-anchored immediate-parent identity marker;
- replacement immediate parent rejected before bootstrap/mutation;
- deterministic recovery to original registry history after operator repair.

Residual namespace ownership/mount stability above the stable grandparent remains
target-host proof, not repository proof.

### Blue final integration candidate

Branch:

`blue/gate-b-f11-final-integration-2026-09-21`

Current integration HEAD:

`4f26c1f015efb8c3530aeba8b3f87a81b0361a3f`

Integration handoff:

`handoff/BLUE_GATE_B_F11_FINAL_INTEGRATION_RECEPTION_2026-09-21.md`

Exact-head CI observed at this checkpoint:

`35619545254 = IN_PROGRESS`

Therefore CURRENT durable state is:

```text
BLUE_F11_FINAL_INTEGRATION = CANDIDATE_CREATED
PASS_FOR_INDEPENDENT_ASTRA_RECHECK = PENDING_EXACT_HEAD_CI
ASTRA_F11_RECHECK_AUTHORIZED = FALSE
```

Do NOT launch independent Astra until the integration exact-head CI is confirmed
`COMPLETED / SUCCESS` and Blue commits the final dispatch.

### Astra branch state

Authoritative intended branch:

`astra/gate-b-f11-lock-identity-recheck-2026-09-21`

At this checkpoint it does NOT yet exist durably on GitHub.

Any local/ad-hoc Claude session branch is not a substitute for the Blue-dispatched
Astra branch and must not be treated as independent authority.

### Next Rail-A actions

After `35619545254 = COMPLETED / SUCCESS`:

1. Blue records:
   `PASS_FOR_INDEPENDENT_ASTRA_RECHECK`;
2. Blue creates exact Astra branch from the authorized integration SHA;
3. Blue commits Astra mission and records `EXPECTED_ASTRA_MISSION_HEAD`;
4. Astra performs TARGETED recheck only:
   - public lock;
   - lock + anchor;
   - whole immediate parent replacement;
   - zero mutation on rejected parent;
   - deterministic single-history recovery;
   - ordinary real process serialization;
   - A1-A5 and A9-A10 targeted non-regression;
   - A6-A8 prior-proof preservation if unchanged;
5. if Astra PASS, Blue receives/converges and proceeds into final Gate-B activation
   binding/seal flow.

Do not restart the whole run-authority architecture audit.

## 5. Gate-B activation pack already prepared

Current prepared downstream authority:

`governance/BLUE_GATE_B_FINAL_ACTIVATION_CONVERGENCE_PACK_2026-09-21.md`

Status:

`BLUE_GATE_B_FINAL_ACTIVATION_CONVERGENCE_PACK = PREPARED / WAITING_FOR_FINAL_F11_ASTRA_PASS`

This pack is non-authorizing until final F11 independent closure.

Important already-closed/prepared domains include:
- F1 evidence-schema recheck PASS;
- F5 Route-1 feasibility closed;
- V4 materialization/activation prestage ready;
- operator-pack Red Team complete;
- M1-M4 repair mechanisms integrated prior to F11;
- exact activation-machine schema/canonicalization/order prepared.

Even after repository F11 PASS:

`TARGET_HOST_READY`, `GATE_B_PASS`, and `t0` remain separate later decisions.

## 6. Rail B — current Product/Economic state

Canonical Product authorities remain:

Forward:

`parallel/claude-forward-data-2026-09-20@83521dbfdd90027c90d04adfb7d814593c2355c5`

Economic:

`parallel/claude-economic-v2-2026-09-20@35dff27b8fac53618da434ee6d31febbddcc0e69`

Blue Product spine reference:

`cbf30c1bb38dd69c11fb64c523c3aec694116b9c`

Interface map:

`builder/post-p0-vertical-interface-map-2026-09-21@8d40af1f2333474d8f26509b61cb257d96f8772d`

Antigravity vertical design:

`parallel/antigravity-post-p0-vertical-shadow-loop-design-2026-09-21@568eea1e028302e14f96a06eb2515bb89aa73ad4`

### Build-prep delivery

Branch:

`parallel/claude-post-p0-vertical-build-prep-2026-09-21`

Final HEAD:

`ce8b1ffe1e09d58162d96f52bea3b10aac1fb6ec`

Status:

`CLAUDE_VERTICAL_BUILD_PREP = READY_FOR_BLUE_REVIEW`

Blue received it as:

`CLAUDE_VERTICAL_BUILD_PREP = ACCEPTED_AS_POST_P0_IMPLEMENTATION_PREP`

The mission changed only its handoff, no Product code.

### Independent adversarial challenge

Advisory challenge branch:

`claude/confident-mendel-h4qqo4@9e6431dedaa621d58218f6ffb09ed1300de1bd1b`

Handoff:

`handoff/CLAUDE_POST_P0_VERTICAL_ADVERSARIAL_CHALLENGE_2026-09-21.md`

Advisory verdict:

`VERTICAL_CHALLENGE = PASS_WITH_REQUIRED_PREBUILD_CORRECTIONS`

Blue consolidated the challenge into authoritative correction spec:

`governance/BLUE_POST_P0_VERTICAL_PREBIGBUILD_CORRECTION_SPEC_2026-09-21.md`

The advisory branch itself has no ongoing authority after reception.

## 7. Rail B — Blue-resolved architecture

### Research state / Economic promotion

Current Research code auto-promotes `VALIDATED -> SHADOW`; that future behavior must
be removed.

Blue resolved the challenged lifecycle circularity as:

```text
RESEARCH
  -> VALIDATED
  -> ECONOMIC ASSESSMENT
       -> NO_TRADE / KILL
            => remains non-tradable
       -> CONTINUE + capital-order-eligible
            -> persist AssessmentRecord
            -> VALIDATED -> SHADOW
            -> persist StrategyRegistry transition
            -> Desk
                 -> SIZE
                 -> RISK
                 -> FILL | VETO | NO_TRADE
```

`SHADOW` means:

scientifically validated + economically admitted for shadow trading.

It does NOT mean a fill has already occurred.

This is required because current `CapitalDesk.actionable()` only selects:
- tradable strategies; or
- evaluation-track strategies.

A normal passed `VALIDATED` strategy is not currently Desk-actionable.

Therefore initial Economic promotion must happen before current Desk actionable/ledger
selection, not halfway through `CapitalDesk._run_strategy()`.

### Sizing

For a durably SHADOW strategy:

```text
available_lane_capital
  = decision_NAV * strategy_allocation

economic_margin_notional
  = MarginSizingRule(available_lane_capital, economic margin)

desk_lifecycle_cap_notional
  = available_lane_capital * definition.capital_fraction

FINAL_SIZE
  = min(economic_margin_notional, desk_lifecycle_cap_notional)
```

Economic size is opportunity-sizing authority.

Lifecycle fraction is only an upper cap.

Risk remains independent downstream veto/throttle.

### Execution

The sole Book-feeding shadow execution authority remains:

`src/quant/desk/execution.py :: ExecutionModel.fill`

Economic opening/execution models are modelling inputs only.

Execution-cost consistency requires:
- pre-sizing model-envelope check against the actual Desk `ExecutionModel`;
- post-sizing implied-participation recheck before fill.

### One Book

No second Book/ledger is authorized.

Existing Book remains authoritative.

## 8. Rail B — accepted pre-build corrections

The future ONE BIG BUILD must include or respect:

1. Economic promotion before Desk actionable selection;
2. actual `DeltaCoordinateBinding` verification through
   `evaluate_delta_coordinate`;
3. session-scoped Forward-confirmation identity rather than whole-ledger identity;
4. post-sizing participation/cost-consistency verification;
5. durable Learning idempotence independent of the capped last-200 display list;
6. explicit provenance:
   `ForwardObservation -> ResearchTicket -> AssessmentRecord -> OpportunityTicket -> DeskJournal -> Ledger -> LearningStore`;
7. expanded deterministic E2E/restart false-positive matrix;
8. no whole Forward/Economic branch merge;
9. no second sizing, execution, Book or scheduler authority.

Known deferred limitation:
current `opportunity_id:symbol` Book operation identity is adequate only for the first
vertical slice's one-fill-per-symbol shape. General follow-up partial fills remain
outside first-slice scope.

## 9. Rail B — current single remaining non-code prerequisite

Current status:

`REAL_RESEARCH_TO_FROZEN_EFFECT_ESTIMATE = MISSING_SCIENTIFIC_OUTPUT_CONTRACT`

Blue dispatched:

`parallel/claude-research-frozen-effect-estimate-prestage-2026-09-21`

Mission HEAD:

`866e6c1e96ab7460a7cd6464ead922e710ce7b0b`

Mission:

`handoff/CLAUDE_RESEARCH_FROZEN_EFFECT_ESTIMATE_PRESTAGE_MISSION_2026-09-21.md`

Authority:

`governance/BLUE_RESEARCH_FROZEN_EFFECT_ESTIMATE_PRESTAGE_2026-09-21.md`

Mission type:

`READ_ONLY_ANALYSIS + SCIENTIFIC_CONTRACT_PREP`

No implementation is authorized.

The mission must determine whether existing science/Research code lawfully produces
the frozen Economic effect coordinate including:
- delta_hat;
- lower/upper;
- confidence semantics;
- clustering provenance;
- exact DeltaCoordinateBinding;
- SPY/security return conventions;
- sample/PIT provenance;
- allocation-constructor identity;
- proof weights precede outcomes.

Allowed final outcomes:

```text
SCIENCE_EFFECT_ESTIMATE_CONTRACT = READY_FOR_BUILD_SPEC
SCIENCE_EFFECT_ESTIMATE_CONTRACT = READY_WITH_BOUNDED_OUTPUT_EXTENSION
SCIENCE_EFFECT_ESTIMATE_CONTRACT = BLOCKED_MISSING_SCIENTIFIC_ESTIMATOR
```

## 10. One-big-build rule

Do NOT dispatch multiple small Product implementation builders.

After the science contract returns and Blue receives it:

1. reconcile it against the pre-big-build correction spec;
2. close remaining exact scientific contract questions;
3. freeze one final vertical-loop Builder specification;
4. create exactly one primary branch:
   `builder/post-p0-first-vertical-shadow-loop-2026-09-21`;
5. implement the coherent vertical slice in one bounded build;
6. require deterministic E2E/restart/adversarial tests;
7. receive targeted independent/adversarial review afterward.

Until the science contract is closed:

`VERTICAL_LOOP_BUILD_SPEC = NOT_FROZEN`

`PRODUCT_IMPLEMENTATION_AUTHORIZED = FALSE`

## 11. Work-allocation discipline

Current intended substantial concurrency:

```text
RAIL A = F11 final integration / Astra dispatch / Gate-B critical path
RAIL B = scientific frozen EffectEstimate prestage
```

Completed Claude design/challenge sessions do not count as active implementation rails.

Do not launch:
- another F11 Builder;
- another competing Product Builder;
- actual Product implementation;
- target-host Gate-B mutation;

unless Blue explicitly changes this routing.

## 12. Immediate Blue watch list

On every resume, check in this order:

1. live Blue master HEAD;
2. F11 final integration branch HEAD + exact-head CI;
3. if F11 integration CI green, materialize final Astra dispatch immediately;
4. scientific prestage branch HEAD + handoff status;
5. receive scientific result and decide build-spec freeze;
6. keep Gate-B and Product execution authority separate.

Do not wait on one rail when safe non-mutating work on the other is independent.

## 13. Current role ownership

```text
BLUE_NOW =
  receive/dispatch authority and keep both rails converged

BUILDER_F11 =
  DONE / STOPPED at e600295b...

ASTRA_F11 =
  NOT YET AUTHORIZED / branch not yet durably created

CLAUDE_VERTICAL_BUILD_PREP =
  DONE / RECEIVED

CLAUDE_VERTICAL_ADVERSARIAL_CHALLENGE =
  DONE / RECEIVED / ADVISORY CONSUMED

CLAUDE_SCIENCE_PRESTAGE =
  ACTIVE on parallel/claude-research-frozen-effect-estimate-prestage-2026-09-21
```

## 14. Do not declare by implication

Do not declare any of the following from repository-only progress:

```text
TARGET_HOST_READY = TRUE
GATE_B_MUTATION_AUTHORIZED = TRUE
GATE_B = PASS
GATE_B_PASS_DECLARED = TRUE
t0 = DECLARED
PRODUCT_INTEGRATION = READY
REAL_CAPITAL_AUTHORIZED = TRUE
```

## 15. Resume rule

A new Blue conversation should begin by reading:

1. `QUANT_NORTH_STAR.md`
2. `handoff/BLUE_PROJECT_REACQUISITION_2026-09-21.md`
3. `governance/CURRENT_GOVERNANCE_STATE_2026-09-20.md`
4. `handoff/BLUE_MASTER_V2_STATE_2026-09-20.md`
5. `NEXT_BUILD_MISSION.md`

Then refresh GitHub and act from live HEAD/CI rather than stale embedded status.

